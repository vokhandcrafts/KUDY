.idea/

# G00.02 spike: generated by scripts/prepare-data.mjs (activated package copy), never tracked
spikes/G00.02-offline-map/runtime/

# G00.04.b root scaffold: dependencies, Expo dev-client cache, local builds, local env values
node_modules/
.expo/
dist/
.env
.env.*
!.env.example
*.tsbuildinfo

# G06.09.a: expo prebuild output (AC5), generated per check, never tracked
/android/
/ios/

# G02.03: build-bundle output (public/private trees, release artifacts), never tracked
tools/build-bundle/build/

# Mimosa security scans: local scan working dir and history, never tracked
.mimosa/

# G17.01.a: collector runtime database (default --db location), never tracked
tools/collector/runtime/


# G05.05.a: the showboat demo's temporary mutation copy of the source trees,
# created and removed inside the demo block (implementation-rules 5: a source
# mirror ships with its ignore entry in the same change)
.tmp-g0505a-proof/
