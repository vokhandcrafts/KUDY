# G18.03 — Module surface reader (`tools/arch-surface`)

**Source:** [adoption plan](../../plans/2026-09-21-zcode-lessons-adoption.md), task 3 ·
[19](../../architecture/19_class_and_module_map.md)

## Goal

A small script that, given a directory, prints its files, their exports and their
external imports — generated **live facts** from the actual code, usable next to
19 and never instead of it (19 is the designed map; this is the generated view
that keeps review honest about drift).

## Constraints

- `tools/arch-surface/`, `node:` APIs only, **zero new dependencies** (the plan is
  explicit). Line-based heuristic extraction is acceptable and must be declared as
  heuristic in the README: the check for correctness is the hand verification
  below, not parser completeness.
- Output is plain text that an agent can paste into a prompt.

## Acceptance criteria

1. Running the script on `core/discovery` and on `services/contentRepo` produces
   the correct file/export/import lists — verified by hand against the code, with
   the comparison recorded in the results file.
2. A test on a fixture directory (committed fixtures) asserts the printed surface
   for known inputs; the test file is wired into the `npm test` glob.
3. The output is deterministic plain text (stable ordering, no timestamps).
4. No new dependencies: `package.json` is unchanged apart from (at most) the test
   glob entry.

## Out of scope

- dependency-graph analysis or rule enforcement (dependency-cruiser from G18.01
  already owns that);
- renderers beyond plain text, watch mode, caching;
- edits to 19 itself.

## Proof

`node --test "tools/arch-surface/*.test.mjs"` — fails if the printer or its
fixture assertions are reverted.

## Notes for the executor

Blocked-by G18.01 and G18.02 by plan order («Задача 1 → Задача 2 → Задача 3»); the
script itself has no technical dependency on them. The README is human-facing:
Belarusian.
