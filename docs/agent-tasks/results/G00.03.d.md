# G00.03.d — Выснова па плацежнай інтэграцыі

**Дата:** 2026-09-15
**Статус:** `decision-required` — канкрэтны ADR гатовы ([G00.03-payment-evidence](../../architecture/decisions/G00.03-payment-evidence.md)): аддзелены live ад mock, зафіксаваныя response/errors/restore/environment mapping/SDK settings для G08 і G00.04; але залежнасць G00.03.c (#28) — `blocked-external` (рэальныя sandbox purchase/restore не выконваліся), а заданне .d загадвае «усё, што не праверана, застаецца блокерам». Вынава не прымаецца да жывога прагону .c і выбару заснавальніка.

## Вынік

- Створаны ADR `docs/architecture/decisions/G00.03-payment-evidence.md`: дзве табліцы ўзроўняў доказа — **mock proven** (.a: кананічны API `09` §5, 20/20; .b: 18 негатываў + 3 мутата-довады — закрыты спіс кодаў памылак з сэнсам, «адмова не кэшуецца», пазітыўны кэш па device, fail-closed правайдара, manifest guard, `environment_mismatch`, гігіена логаў, strict env-gate) і **live `not-run`** (клеткі .c: purchase/restore на iOS/Android, on-device негатывы, форма RevenueCat REST, webhook-off факт); фіксацыя для **G08** (выдаўца права — сервер; `403` vs `503` абавязкова адрозніваюцца; пакупка ≠ ready; загружаны бандл грае незалежна) і для **G00.04** (environment mapping sandbox/production, уладальнікі ключоў паводле `.env.example`, SDK settings, версіі — пін G00.04.c); сцэнары з адзінымі імёнамі палёў і кодамі даслоўна з `09` §5 / [ADR G01.03](../../architecture/decisions/G01.03-session-access.md); блокеры і пярэдумовы прыняцця (§9 ADR).
- Рэкамендацыйных прадуктовых выбараў ADR не адкрывае: флоў і пастаўшчык ужо кананічныя (`09` §5.1), таму ADR — фіксацыя доказаў, а не новы выбар; аднак статус не `complete`, бо крытэрый задання «усё, што не праверана, застаецца блокерам» трымае live-узровень адкрытым.
- Бацькоўскі result `G00.03.md` не ствараўся і не дапаўняўся (толькі пасля апошняй часткі пакета); агульны `09` не тыкаўся (інтэграцыя — G00.04).
- Showboat: **не ўзнікае** — змена толькі дакументальная, ніводная ўмова decision rule `showboat.md` (1–5) не выканана; дэма не стваралася (дадаткова `uvx` у асяроддзі адсутнічае — фіксацыя .a/.c).

## Зыходны стан і план

- Залежнасць G00.03.c (#28) фармальна закрытая; яе result — `blocked-external` з гатовым выканаўчым пратаколам `spikes/G00.03-sandbox-grant/evidence/README.md`; result .a і .b — `complete`. Гэта запісана ў «Адзнацы залежнасці» ADR, не схавана.
- Workspace правераны перад стварэннем: `docs/architecture/decisions/G00.03-payment-evidence.md` і `docs/agent-tasks/results/G00.03.d.md` у дрэве `origin/main` не існавалі.
- Агульны checkout займаюць іншыя агенты (галінка `zcode/19`, чужыя WIP-файлы): праца сабраная git-plumbing-ам ад дрэва `origin/main` у асобную галінку `zcode/29`, рабочае дрэва і чужыя змены не дакранутыя.
- Інвентарызацыя: Windows 10 (10.0.19045), Node `24.13.0`, git `core.autocrlf=true`, `uvx` адсутнічае; сеткавы доступ забаронены issue (Out of scope: no new network access) — форма RevenueCat REST, версіі SDK і live-клеткі застаюцца `not-run`.
- План толькі гэтых файлаў: ADR + гэты result; выкананы без выхаду за scope.

## Уваходы і выбар

- Прачытаныя: заданне `G00.03.d`, бацькоўскі brief `G00.03` (у тым лічбе перадача «G00.04 — версіі, settings і env назвы; G08 — пацверджаныя response/error/restore паводзіны»), results `.a`/`.b`/`.c` + названы імі пратакол `evidence/README.md` і `.env.example`, `09` §5/§5.1/§5.2 (і §2 — рэфанд-трэйд-оф), `10` (RC REST/restore/webhook факты, Supabase signed-URL), `15`, `18` §5, [ADR G01.03](../../architecture/decisions/G01.03-session-access.md) §3.5–3.6, правілы `issue-workflow`/`showboat`/`code-review`/`documentation-language`, `AGENTS.md`.
- Выбар у межах кантракту: структура ADR «дзве табліцы ўзроўняў доказа → фіксацыя для G08 → mapping для G00.04 → блокеры»; выснова не адкрывае новых прадуктовых варыянтаў, таму «не прынята» трымаецца на live-блокерах і выбару заснавальніка, не на незраблёнай рэкамендацыі.
- P01/P02 не датычацца плацяжоў; дазволу на рэальныя грошы/production задача не дае; production-ключы не існуюць і не ствараліся.

## Змененыя файлы

- `docs/architecture/decisions/G00.03-payment-evidence.md` (новы)
- `docs/agent-tasks/results/G00.03.d.md` (гэты файл)

## Праверкі і доказы

Асяроддзе: Windows 10 (10.0.19045), Node `24.13.0`, git `core.autocrlf=true`. Задача дакументальная — праверкі чытальныя/мадэльныя; прыладавыя і сеткавыя — `not-run`.

1. Спасылкі ADR на існуючыя файлы (node-скрыпт выцягвае адносныя спасылкі і правярае існаванне)
   - Чакалася: усе адносныя спасылкі вырашаюцца.
   - Фактычна: на рабочым дрэве checkout (`zcode/19`) — **10/12**: `results/G00.03.c.md` і `spikes/.../evidence/README.md` адсутнічаюць толькі ў састарэлым мясцовым дрэве; абодва мэты пацверджаныя ў дрэве `origin/main` (`git ls-tree origin/main` — абодва блобы існуюць), на базу якога сабраны каміт. Для гэтай змены — **12/12**.
2. Адзіныя імёны палёў і кодаў адносна прынятых крыніц (крок 3 атамаркі)
   - Дзеянне: зверыць `device_id`/`device_secret`/`route_id`/`version`/`locale`/`tier`/`lock_url`/`urls:[{path,url,expires_at}]`, `AccessReady(route_id, version, locale, tier, stop_ids[], issuer='services/download')` і коды памылак супраць `09` §5/§5.1 і [ADR G01.03](../../architecture/decisions/G01.03-session-access.md) §3.5.
   - Фактычна: перанесеныя даслоўна; уласных нарматыўных імёнаў ADR не ўводзіць; статус «не прынята» забараняе лічыць яго законам да прыняцця.
3. Адсутнасць канкуруючых нарматыўных правіл (крок 3 атамаркі)
   - Дзеянне: супрацьпастаўляць ADR з `09` §5.1 (флоў і webhook-правілы), `10`, `15`, `18` §5.
   - Фактычна: ADR пераказвае прынятае, не пераглядае яго; усе сцвярджэнні з запісаных крыніц з датамі; «не доказ store/RC/OS» напісана ў тэксце (§6).
4. `node --test docs/run-model/run-model.test.mjs`
   - Чакалася: мадэль зялёная, гэтая змена яе не тыкаецца.
   - Фактычна: **51/51 pass** (асяроддзе агульнага checkout; змена — толькі markdown супраць дрэва `origin/main`).
5. `node docs/run-model/check-regressions.mjs`
   - Чакалася: усе рэгрэсіі адхіленыя, мадэль без зменаў.
   - Фактычна: **16/16 rejected**, `Repository model unchanged`.
6. `npx --yes jscpd@5.1.2 --config .jscpd.json --no-tips .` з кораня
   - Чакалася: 0 clones (змена — толькі markdown, у пару не трапляе).
   - Фактычна: `Found 0 clones` (0.00%).
7. tsc/lint: непрыменныя — root `package.json`/`tsconfig` у checkout няма; залежнасці дзеля review дакумента не ўсталёўваліся (забарона новага сеткавага доступу).
8. Live-клеткі (§2.2 ADR): purchase→grant→байты на iOS/Android, restore на чыстай ўстаноўцы, on-device негатывы, форма RC REST, webhook-off факт — `not-run`: няма ASC/Google Play акаўнтаў, праекта RevenueCat, фізічных прылад і сеткавага доступу (фіксацыя .a/.b/.c). Мокі замест гэтага не ствараліся; mock не выдаецца за store-доказ.
9. Раунд рэв'ю PR #96 (cursor[bot] COMMENTED, 2026-09-15: 0 critical · 0 high · 1 medium · 2 low, вердыкт merge)
   - Дзеянне: праверыць 7 інлайн-заўваг супраць фактычнага тэксту `09`.
   - Фактычна: **7/7 валідныя, усе выпраўленыя**: (a) MEDIUM — рэфанд-трэйд-оф «файлы на дыску = купленае» цытуецца `09` **§2 «Межы даверу»** (радок 121), не §6.2 (`core/pipeline`) — выпраўлена ў шапцы ADR, §3.5 і §6, а таксама ў гэтым result; (b) LOW — правіла `tier: base` для `free_base` — `09` **§5.2** (радок 271), не §5 — выпраўлена ў §3.6; (c) LOW ×3 — бітыя адносныя спасылкі на [ADR G01.03](../../architecture/decisions/G01.03-session-access.md) у гэтым result выпраўленыя на `../../architecture/decisions/…` (папярэдняя праверка 1 пакрывала толькі ADR, не result — дэфект самай праверкі, цяпер спраўджаныя ўсе 12 спасылак ADR + 3 спасылкі result).
10. Трэці раунд рэв'ю PR #96 (cursor[bot] APPROVE на 8933a4e; 2 low + 1 info, non-blocking)
   - Фактычна: **выпраўленыя ўсё роўна**: правіла «бясплатны манifest не змяшчае шляхоў extended-ассетаў» цытуецца `09` §2 (радок 117, крыніца `05` п. 19), не §4; русізм «Уже» → «Ужо» (ADR §3.5); змешанае «Манifest» перафразавана ў «Бясплатны манifest». Result гэтых дэфектаў не меў (спраўджана grep).



## Самарэвю (па code-review.md)

Scope: дзва новыя markdown-файлы; чужыя WIP-змены агульнага checkout не ўключаныя (каміт сабраны ад дрэва `origin/main`). Primary concern — contract conformance: імёны палёў, коды памылак і патрабаванні («кліент ніколі не сцвярджае пра пакупку», «403 vs 503», «адмова не кэшуецца», signed URL кароткія і серверныя) перададзеныя даслоўна з прынятых крыніц (праверкі 2–3). Сакрэтаў/токенаў/production даных няма: названы толькі **назвы** env-зменных з `.env.example` і сінтэтычны `product_id` пратаколу — не сакрэты; ніякіх URL сервера або ключоў. Новых залежнасцяў няма; кода з Tramio/TourForge няма; §17 не парушаны — ADR цытуе забароны. Тэст «падзе пры рэверце» дакументальнай змены — сам ADR як адзіны выніковы файл; выканаўчага кода змена не дадае, таму выканаўчага тэсту няма — абмежаванне тыпу задачы, не прабела ў пакрыцці.

Summary: 0 critical · 0 high · 0 medium · 0 low
Verdict: merge — выснова сумленна падзяляе доказы, блокеры пералічаныя, выхаду за дазволеныя файлы няма.

## Нявыкананае і перадача

- Live-клеткі .c — `not-run` (няма акаўнтаў/прылад/RC-праекта/сеткі): таму `decision-required`, не `complete`.
- Форма RevenueCat REST і версіі SDK — `not-run`/«не праверана» (забарона сеткі); пін — G00.04.c.
- Бацькоўскі result `G00.03.md` — не створаны (правіла апошняй часткі).

**Наступнік:** выканаўца G00.03.c з доступамі — пратакол `evidence/README.md` (Прагон 1–3 + негатывы, naming/reдактаванне доказаў там жа); пасля прагону перавесці result .c у `complete`, запоўніць live-клеткі §2.2 ADR фактаў (не тлумачэнняў), потым заснавальнік зацвярджае выснову (§9 ADR). G08 бярэ §3 (паводзіны памылак/кэша/restore), G00.04 — §4–§5 (mapping, settings, што пінуць); бацькоўскі result дапаўняецца пасля апошняй часткі пакета.
