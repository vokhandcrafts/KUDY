# G01.01.b result — Story-level progress model (ADR variant A)

**Status:** `complete`

The founder accepted variant A on 2026-09-14 (recorded in issue #12 and in the ADR header per its §7 founder action). This part implemented exactly ADR §4 in the executable documentation model, with the base→extended scenario demonstrated as failing on the previous stop-keyed model before implementation.

## Changed files

- `docs/run-model/run-model.mjs` (rewritten to the ADR §4 contract)
- `docs/run-model/run-model.test.mjs` (rewritten: story-level scenarios + carried-over invariants)
- `docs/run-model/check-regressions.mjs` (12 mutations, 2 new)
- `docs/run-model/README.md` (model semantics, limits, counts)
- `docs/architecture/decisions/G01.01-narration-progress.md` (header status → accepted, variant A; §7 marked executed — the founder's §7 action recorded on the founder's explicit instruction)
- `docs/agent-tasks/results/G01.01.b.md` (this file)
- `docs/demos/2026-09-14-g01-01-b-story-progress.md` (Showboat demo)

Not changed: `09`, `11`, `13`, `14`, `15`, `16`, parent `docs/agent-tasks/results/G01.01.md`, issue title/body/criteria.

## Inputs

| Input | Fact |
|---|---|
| Founder decision | variant A accepted 2026-09-14 (issue #12 comment thread; ADR header updated) |
| ADR | `docs/architecture/decisions/G01.01-narration-progress.md` §4 (canonical contract), §5 (acceptance scenarios) |
| Predecessor result | `docs/agent-tasks/results/G01.01.a.md` — `decision-required` lifted by the founder choice |
| Model baseline | Node v24.13.0, no root `package.json`, previous model: 41 tests green, 9 mutations |
| Environment | Windows, Git Bash, working checkout `C:/Users/kamyl/WebstormProjects/KUDY`, branch `zcode/12` from `origin/main` |

## Checks

| Action | Expected | Actual | Evidence |
|---|---|---|---|
| New suite on the OLD model (fail-first) | new ADR scenarios fail | 41 tests: 27 pass, 14 fail; `G01.01.b: base heard stays heard…` — `undefined` vs `'story-crane-base'` | `node --test --test-reporter=spec docs/run-model/run-model.test.mjs` on pre-change `run-model.mjs` |
| Full suite on the new model | all scenarios pass, incl. interrupted replay, paid-only unlock, manual-primary-before-approach, queue-vs-heard, foreign completion | 41 tests: 41 pass, 0 fail | same command, current tree |
| Bounded exploration | monotonic `heard` (story ids) and `autoFired` (stop ids), single PlayStory per step, Ended terminal | 7 380 transitions (9 event choices, depth 4), all invariants hold | `assert.equal(transitions, 7380)` in the suite |
| Mutation gate | every reviewed defect rejected | 12/12 caught, incl. `credit extended together with base` and `swap primary to extended after unlock` | `node docs/run-model/check-regressions.mjs` |
| ADR §5 row 1 behavior | base heard stays heard; no Play from unlock; marker `played`; extended listed as open discovery | exact match (script output in demo) | Showboat demo block 3 |
| Showboat | demo created and verified per policy (condition 4: state-contract change) | `uvx showboat==0.6.1 verify docs/demos/2026-09-14-g01-01-b-story-progress.md` → exit 0 | demo file |
| jscpd gate | 0 clones | `npx --yes jscpd@5.1.2 --config .jscpd.json --no-tips .` → `Found 0 clones` (0.00%) | gate output |
| `npx tsc --noEmit` / `npm run lint` | N/A without root `package.json` | not run; dependencies not installed for a review document | `test -f package.json` → absent |
| OS player / store / SQLite | not claimed | not run — documentation model only | README «Межы» |

## Code review (own diff)

Scope: 6 files, +261/−54. Lead concern: contract conformance with ADR §4.

Pre-checks: `git diff --numstat` (+261/−54); no stray commits (work uncommitted at review time); no secrets; no new dependencies; `tsc`/`lint` N/A (no root `package.json`); forbidden list of `09` §17 not applicable (no runtime, no network, no storage in the model).

Field names follow the ADR one-to-one (`heard` story ids, `autoFired` stop ids, `playing {stopId, storyId, playId}`, `queued {stopId, radius, at}`, `accessibleStopIds`, `tierAvailable`, `storyBaseId`/`storyExtendedId`); `primary` stays derived, never stored. Layer boundaries hold: pure reducer, no I/O. Purchase/AccessReady cannot start audio (asserted). `AccessReady` is atomic (unknown stop or bogus tier rejects the whole event). Local progress stays consent-independent (no consent input exists).

```
Summary: 0 critical · 0 high · 0 medium · 0 low
Verdict: merge — model matches accepted ADR §4; every §5 scenario is asserted and mutation-guarded
```

Known modeled-out limits (documented in README, unchanged from predecessor): locale not simulated (session pins version only in the model), no OS player/store/SQLite, `missed()` keeps its legacy helper name for the story-unit discovery list.

## Not done

- Sync of `09` / `11` / `13` / `14` / `15` / `16` to the accepted semantics — G01.01.c.
- Owner of sound, Moments, tagged callbacks — G01.02.
- Durable SQLite schema and migration of stored `heard[]` (stop→story) — G01.03.
- Parent result `docs/agent-tasks/results/G01.01.md` — only after all G01.01 parts are `complete`.
- No real device, store or server claims.

## Successor input (G01.01.c)

1. The ADR (`docs/architecture/decisions/G01.01-narration-progress.md`) is now **accepted, variant A**; §4 is law for progress fields. Sync `09` (§3, §6.1, §7), `11` (§3, §5, §6, §12 — keep C-ids, mark changed sense in coverage), `13` §4, `14` open question, `15` P01 status → accepted, and the corresponding `16` rows to story-level semantics.
2. Canonical vocabulary after acceptance A: `heard: Set<story_id>`; `auto_fired: Set<stop_id>`; derived `primary_story_id` / `additional_story_id`; `story_accessible` from `accessible_stop_ids` + `tier_available`; `UserSelectedStory(stop_id, story_id)`; markers are stop-unit from the primary; «Яшчэ можна адкрыць» is story-unit, locked excluded. Old `heard: Set<stop_id>` rows become history.
3. Executable reference for the semantics: `docs/run-model/run-model.mjs` + its test suite; behavioral demo: `docs/demos/2026-09-14-g01-01-b-story-progress.md`.
4. Do not touch `docs/run-model/*` files or this result when syncing documents; they are the recorded evidence of G01.01.b.
