# G01.06 result — Discovery and feedback contract fixtures

**Status:** `complete`

All five acceptance criteria of `docs/agent-tasks/discovery/G01.06.md` are satisfied with machine-checked fixtures; the schema limits and the catalog-envelope decision are synchronized into `docs/architecture/21_discovery_feedback_architecture.md`. No runtime implementation was written, per the brief; downstream production tasks (G02.01 and later) stay gated by their existing G00/G01 predecessors.

## Changed files

- `fixtures/discovery-contract/index-valid.json` (created) — normative `DiscoveryIndexV1` with one free place, one paid guide, one free sea place with unknown duration, one en-only place and one mixed collection offer
- `fixtures/discovery-contract/catalog-legacy.json` (created) — catalog envelope v1 without any discovery pointer; valid input for guides only
- `fixtures/discovery-contract/catalog-with-discovery.json` (created) — same envelope plus `discovery_index` pointer whose `bytes`/`sha256` were computed from `index-valid.json` (over the LF git blob; verify via `git show`, not the platform working copy, because Windows autocrlf checkouts change the file bytes)
- `fixtures/discovery-contract/index-invalid-duplicate-ref.json` (created) — duplicated collection member ref
- `fixtures/discovery-contract/index-invalid-nested-collection.json` (created) — collection member of kind `collection`
- `fixtures/discovery-contract/index-invalid-unknown-locale.json` (created) — `ua` outside the BCP-47 allowlist
- `fixtures/discovery-contract/index-invalid-private-path.json` (created) — paid guide offer whose `detail_ref` carries `place_public` kind and a path into `private/…/extended/…`; both violations are named in the fixture text
- `fixtures/discovery-contract/index-invalid-unknown-season.json` (created) — invented season value `all`
- `fixtures/discovery-contract/index-invalid-duration-range.json` (created) — `min_minutes > max_minutes`; the fixture text states the companion rule that a guide range must include the route `duration_min`
- `fixtures/discovery-contract/index-invalid-foreign-city.json` (created) — offer `city_id` differs from the index city
- `fixtures/discovery-contract/feedback-cases.json` (created) — 14 request/response cases: create, lost-ACK retry, edit, stale revision 409, CAS delete tombstone, read-after-delete, second device own key, cross-device 409 and own-empty read, wrong-kind reason, imported target, score out of range, more than 3 reasons, deleted device 401
- `docs/architecture/21_discovery_feedback_architecture.md` (edited) — envelope decision, schema limits, field trace, registry revision rule, fixture references
- `docs/agent-tasks/results/G01.06.md` (this file)

Not changed: `09`, `11`, `13`, `14`, `15`, `16`, `20`, `run-model/*`, any code, `package.json`, dispatcher or workflow files.

## Inputs

| Input | Fact |
|---|---|
| Predecessor results | none required; task file says "Dependencies: none; planning/contract work may start now" |
| `docs/20_discovery_and_feedback.md` | D01–L02 acceptance rows; button dictionary; P04 replacement |
| `docs/architecture/21_discovery_feedback_architecture.md` (baseline) | §3.1 refs, §3.2 index shape, §3.3 catalog pointer, §4 selector rules, §5 feedback tables/CAS, §5.3 limits 8 KiB / 30–120 rpm |
| `docs/architecture/09_technical_architecture.md` §3, §4, §5, §17, §21 | catalog route-entry fields (`route_id, version, locales, layers, sizes, product_id`), bundle immutability, forbidden list, extension boundary |
| `docs/plans/2026-09-13-discovery-feedback.md` §5, §7 | reference fixture table A–H + feedback scenario; "Only one task writes … `21` … at a time" |
| `docs/16_delivery_backlog.md` | G01.06 has no dependencies; G02.01/G15.01/G16.01 depend on it |
| `docs/02_data_architecture.md` | city_id on all content; no new User account |
| Environment | Windows 10, Git Bash, Node v24.13.0 present; no root `package.json`; work happened on branch `zcode/38` in a linked worktree because a concurrent session was active in the main checkout; the branch is based on `origin/main` 21420a0 (after PRs #40/#41 merged); the first draft of this work was committed from base c22a02f and recommitted onto the new base as `9201a40` before the handback |

Baseline: `fixtures/` did not exist; `21` had no numeric schema limits and left the catalog envelope decision to this task.

## What was decided (for G02.01)

1. **Catalog envelope.** `catalog.json` is a versioned envelope `{catalog_schema_version: 1, generated_at?, routes: [...], discovery_index?}`; route entries keep their `09` §4 shape inside `routes`; city/discovery fields are never added to individual route entries. A catalog without `discovery_index` is a first-class input; unknown top-level fields are ignored; unknown major version means "guides only, no discovery, never blocks Run"; a bare route array (legacy v0) reads as a catalog without discovery. Recorded in `21` §3.3.
2. **Schema limits** (recorded in `21` §3.2, enforced by the fixtures): offers ≤ 64, collections ≤ 32, members ≤ 50 (no duplicates), themes ≤ 32 / ≤ 8 per offer, ≤ 8 locales per localized object (BCP-47 allowlist), title ≤ 140 chars, summary/why_recommended/conditions/description/overlap_note ≤ 400, season reason ≤ 280, `editorial_order` 0–9999, duration integer 1 ≤ min ≤ max ≤ 1440 (guide range must include `duration_min`), `distance_m` ≤ 100000, identifiers and `revision` ≤ 64 chars of `[a-z0-9._-]`, index file ≤ 512 KiB verified by catalog `bytes`/`sha256` before fetch. Payload bound rationale: 512 KiB parses in single-digit milliseconds at city entry, ≲ 2 MiB transient heap, cache swap is one catalog pointer.
3. **`detail_ref` kinds:** `place_public`/`collection_public` (safe relative path, no `private`/`extended`/`..` segments) and `guide_preview` (no path; the known route preview). Private transcripts, extended media paths, full paid texts and raw ratings never enter the index (`21` §3.2 + §2 trace table).
4. **`overlap_note`:** a collection whose members contain both a guide and a place from its route must carry a localized `overlap_note` saying it is the same experience; its absence is a validator error (`21` §3.2). Exercised by `collection-f1` (members `place-a1` + `guide-route-a1`, treated as guide-through-place).
5. **Registry revisions.** The publisher registers every target actually contained in a release manifest: guide `{route_id, version, locale}` per published text locale, place `{place_id, content_version, locale}` for the published public projection; superseded historical versions stay registered and keep accepting feedback; collections and single stories are not targets this release; `prepared → published` flow unchanged (`21` §5.2).
6. **Unknown stays unknown.** Missing `estimated_duration` and empty `season_recommendations` are the only honest representations of unknown/not-assessed; `null`, invented enums (`all`) and auto-filled defaults are rejected (`21` §3.2, negative fixtures).

## Checks

| Action | Expected | Actual | Evidence |
|---|---|---|---|
| JSON syntax of all 11 fixtures | all parse | all parse | `node -e "for (const f of require('fs').readdirSync('fixtures/discovery-contract')) JSON.parse(require('fs').readFileSync('fixtures/discovery-contract/'+f,'utf8')); console.log('OK')"` → `OK` |
| Structural validation of `index-valid.json` | passes all schema rules of `21` §3.2 | passes (key set, refs per kind, themes, localized limits, duration/distance/order ranges, availability, access, detail_ref, collections incl. `overlap_note`, member refs resolve to published offers, start places resolve) | one-off validator, output `PASS index-valid` |
| Catalog ↔ index consistency | `discovery_index.bytes`/`sha256` equal the committed `index-valid.json` git blob; path city equals index `city_id` | `bytes 9505`, `sha256 ba4a6766…73d260` match the blob; city `city-a` matches | `node -e "const cp=require('child_process'),c=require('crypto');const i=cp.execSync('git show HEAD:fixtures/discovery-contract/index-valid.json');const d=JSON.parse(cp.execSync('git show HEAD:fixtures/discovery-contract/catalog-with-discovery.json')).discovery_index;console.log(d.bytes===i.length, d.sha256===c.createHash('sha256').update(i).digest('hex'))"` → `true true` (run after commit; platform-independent because `git show` returns the LF blob) |
| Negative fixtures are rejected by the named rule | each invalid index fails validation on exactly its violation | duplicate-ref → duplicate member ref; nested-collection → forbidden member kind; unknown-locale → unknown locale `ua`; private-path → unsafe detail_ref path; unknown-season → unknown season `all`; duration-range → duration range invalid; foreign-city → foreign city | one-off validator, 7 `REJECT` lines with matching rules |
| Feedback cases consistency | valid cases respect scale/reasons/targets; sequence revisions 0→1→(1)→2→(409)→3; named outcomes present | all 14 cases checked; lost-ACK reuses `mutation_id`; cross-device 409 + own-empty read; imported target 422; wrong-kind reason 422; score 6 → 422; 4 reasons → 422; deleted device → 401 | one-off validator, `PASS feedback-cases` |
| Full validator transcript | 11/11 groups pass | `PASS` × 3 positive groups + `REJECT` × 7 negative groups + `PASS feedback-cases`, "11/11 fixture groups passed" | one-off script run 2026-09-14; the script is not committed (allowed outputs), its rule set is enumerated above and in `21` §3.2/§3.3 so G02.01 encodes it in the production validator |
| Links and task graph | every local link and G-id referenced from `21` resolves | `../20_discovery_and_feedback.md`, `09_technical_architecture.md`, `fixtures/discovery-contract/` exist; G01.06/G02.01/G02.02/G02.03/G02.04/G15.01/G15.03/G16.01 present in `16` | grep/table checks 2026-09-14 |
| No competing normative text | old "G02.01 defines the envelope" sentence replaced by the decision | `grep -c "Калі цяперашняя схема каталога"` → 0 | workspace grep 2026-09-14 |
| `npx tsc --noEmit` / `npm run lint` | N/A without `package.json` | not run; no dependencies installed | `test -f package.json` → absent |
| jscpd copy-paste gate | pass | pass — 0 clones over 12 files / 1247 lines (formats are js/ts only, fixtures are JSON and markdown) | `npx --yes jscpd@5.1.2 --config .jscpd.json --no-tips .` run 2026-09-14 after the handoff fixes |
| Showboat | docs + JSON fixtures only; none of conditions 1–5 (no runtime source files, no behavior change, no bug fix, no executable contract/migration change, diff has 0 runtime source lines) | not created | `docs/agent-rules/showboat.md`; `git diff --numstat` lists only `.md` and `.json` under docs/fixtures |
| OS / store / server / device | not claimed | not run | — |

## Code review (own diff)

Scope: `git diff --numstat main...HEAD` — 13 files: 11 new JSON fixtures, one edited architecture doc, one new result file. Lead concern: contract conformance with `21`/`09`/`20` and no invented fields.

Pre-checks: no secrets (fixtures are fully synthetic; no keys, tokens, real place names or coordinates); no dependencies added; `09` §17 forbidden list respected (no runtime LLM, no server catalog, no public ratings, no second player/GPS owner, no entitlement from collection).

Findings fixed during the pass:

1. [MEDIUM] `index-valid.json` — `suggested_start_place_id: null` on offers without a start place implied a known "null place". Fixed by omitting the optional field (unknown = absent), consistent with the duration/season rule.
2. [MEDIUM] `index-invalid-duplicate-ref.json` — empty `offers` made the fixture trip the "member is not a published ref" rule before the duplicate rule. Fixed by adding the legitimate `place-a1` offer so exactly one violation remains.

Residual: none. The one-off validator deliberately stays uncommitted (allowed outputs); the normative rule source is `21` §3.2/§3.3 and the per-rule expectations are listed above.

```
Summary: 0 critical · 0 high · 0 medium · 0 low
Verdict: merge — fixtures and limits are internally consistent and synchronized with 21; no runtime claims
```

## Handoff review (second session, 2026-09-14)

The task was handed back after the first session committed its draft and was redirected; a second ZCode session claimed #38, reviewed every file of the handoff commit and finished the task. Scope of the second pass: contract conformance of the fixtures against `21` §3/§5 and `09` §4, internal consistency, and the recorded evidence. Findings fixed in this commit:

1. [HIGH] `catalog-legacy.json`, `catalog-with-discovery.json` — route entries carried a `city_id` field, contradicting the envelope decision itself (`21` §3.3: city/index fields are never added to individual route entries), the `09` §4 route-entry form (`route_id, version, locales, layers, sizes, product_id`) and decision 1 below. Fixed by removing `city_id` from all route entries; city binding stays where the contract puts it — the index `city_id` and the `discovery_index.path` city segment.
2. [MEDIUM] `index-invalid-private-path.json` — the fixture violated two rules (wrong `detail_ref` kind for a guide offer plus a private path) while its text documented only the path. Negative fixtures must fail for the documented reason, so the fixture text now names both violations.
3. [LOW] `index-invalid-duration-range.json` — the text claimed a second violation ("range below the route `duration_min`") that no fixture data can demonstrate, since `duration_min` lives on the route entity, not in these fixtures. The text now states the demonstrated violation (`min > max`) and the companion rule as a rule.

Re-validation after the fixes (one-off validator, uncommitted; 2026-09-14): **44 checks, 0 FAIL, 0 WARN** — JSON syntax of all 11 fixtures; catalog form without city/index fields on route entries; `discovery_index.bytes` (9505) and `sha256` (`ba4a6766…73d260`) still match the untouched `index-valid.json` blob via `git show`; `index-valid.json` accepted by all §3.2 rules incl. the `overlap_note` rule; all seven negative fixtures rejected on their named rules; feedback sequence 0→1→(1)→2→(409)→3 intact with per-method body shapes (`disclosure_version` only on `PUT`, per §5.3); no coordinates, free text or session ids in feedback payloads. jscpd gate re-run after the fixes: 0 clones.

```
Summary: 0 critical · 1 high · 1 medium · 1 low — all fixed in this commit
Verdict: merge — handoff content verified against 21/09/20, contradictions fixed, evidence re-recorded
```

## Not done

- No production contracts (`contracts/discovery.ts`, `contracts/feedback.ts`), selector, validator, build or publish code — G02.01–G02.04 own them and stay blocked by their G01 predecessors.
- No runnable API/DB/RLS proof — §9 of `21` explicitly does not treat this specification as such proof.
- No real city ids, places or author texts — fixtures are synthetic per plan §5 and never claim to be published author choices.
- No Showboat demo — not required by policy.

## Successor input (G02.01)

1. Consume `fixtures/discovery-contract/` as the schema conformance set: `index-valid.json` plus both catalog fixtures must pass the validator; the seven `index-invalid-*.json` must each fail on the named rule.
2. Implement the catalog envelope exactly as decided in `21` §3.3 (versioned object; legacy catalog and legacy bare array stay readable; unknown major version degrades to guides-only).
3. Encode the §3.2 limits verbatim in the content validator; keep feedback limits from §5.3 unchanged (8 KiB, 3 reasons, 30/120 rpm).
4. Do not reuse the synthetic ids (`city-a`, `place-a1`, …) for real content; real publication uses real `city_id` slugs and the author's editorial data.
5. G02.01 remains blocked until G01.01/G01.03/G01.04 parents complete per `16`; this result unblocks only the contract side.
