# G01.01.c result — One narration semantics across the documents

**Status:** `complete`

The documents `09/11/13/14/15/16` now carry exactly the founder-accepted story-level contract (ADR variant A, 2026-09-14) with no competing normative rule left in them. This part changed documentation only; the executable model (G01.01.b) and the ADR text were not modified.

## Changed files

- `docs/architecture/09_technical_architecture.md` — `heard: Set<story_id>` in §6.1 state; derived stop status per ADR §4.5 (`played` = `primary_story_id ∈ heard`, `available` adds the primary-not-heard clause); `queued` shape `{ stop_id, radius, at }`; `UserSelectedStory` added to events and to the `autoplay_suspended` clearing list (invariant 7); invariant 1/4 extended (named-story play; `story_id` must match `playing.story_id`); queue replay requires primary not heard (§4.8.4, §4.9); End list unit = story (§4.6); §6.1 «Мадэль прагрэсу» paragraph replaces the old limitation paragraph (old wording marked as history); §7 `session` table: `auto_fired[]` = `RouteStop.id`, `heard[]` = `Story.id` with the G01.03 migration pointer; §10 `session_ended` signature now carries `reason: user_stop | switch_walk` and `heard_stories_count` (previously prose-only), count unit = stories; §18 item 3 marked closed (base/extended progress contract accepted).
- `docs/11_run_interaction.md` — header note (P01 accepted, P02 still proposal); §3.1 field table (`heard: Set<story_id>`, `playing { stop_id, story_id, play_id } | null`, unit rules, forbidden synonyms); status derivation table per ADR §4.5; manual-repeat paragraph plus the additional-story button rule; «Яшчэ можна адкрыць» redefined as story-unit with the stop-level wording marked as history; §3.2 `nowPlaying` derived from `playing`; §4.3 Finish list unit = story; §5.1 condition 4 = `stop_id ∉ auto_fired` and `primary_story_id ∉ heard` (§4.8.4); §5.1.1 row 4 updated; §5.2 clearing list names both narration roles; §5.3 queue-close outcomes keep the derived status (played wins over available when the primary is already heard; `auto_fired` alone is added) instead of forcing `available`; §6 marker table sources (primary story) + card-state line; §11 mapping table rows updated; §12 criteria 7/11/12 reworded to story semantics (IDs kept), criteria 36–37 added (base→extended unlock; additional story manual-only), no renumbering.
- `docs/13_first_walk.md` — §4 readiness limit marked closed 2026-09-14 with the ADR pointer; the previous open-question wording kept as an explicit history sentence.
- `docs/14_consistency_review.md` — item 1 of «Што яшчэ не закрыта» marked closed (history kept, model scope stated precisely: logic proven by `run-model`, device by G05/G06); the 2026-09-13 P01/P02 sentence rewritten to past tense with the status split named inline; new «Дапаўненне 2026-09-14: наратыў і прагрэс» section.
- `docs/15_guide_decisions.md` — P01 moved out of «Прапанаваныя рашэнні — патрабуюць рашэння» into its own accepted section (`## P01. … — прынята (варыянт A, 2026-09-14)`), status line with ADR + issue #12 pointers, proposal text preserved as history; «Што рабіць наступным» reworded from «закрыць выбар гісторыі» to «выбар гісторыі закрыты» with the ADR link; P02/P03/P04 placement untouched.
- `docs/16_delivery_backlog.md` — dated update note; P01 coverage row marked accepted; coverage row `11` C36–C37 added (G05.01, G05.03, G06.02, G08.04); C31–C35 row unchanged.
- `docs/agent-tasks/results/G01.01.c.md` (this file).

Not changed: ADR, `run-model/*` (42 tests and 12 mutations still green), `18_component_blueprint.md` (its `heard` mentions are ownership boundaries, no keying rule — checked, no competing normative text), parent `docs/agent-tasks/results/G01.01.md` (does not exist and is outside this part's allowed files — see Handover), issue title/body/criteria.

## Inputs

| Input | Fact |
|---|---|
| Founder decision | variant A accepted 2026-09-14 (issue #12; ADR header «прынята, варыянт A») |
| ADR | `docs/architecture/decisions/G01.01-narration-progress.md` — canonical source of progress fields (§4), scenarios (§5) |
| Predecessor results | `docs/agent-tasks/results/G01.01.a.md` (`decision-required` — lifted by the founder choice), `G01.01.b.md` (`complete`, model on ADR §4) |
| Task file | `docs/agent-tasks/atomic/G01.01.c.md`; parent brief `docs/agent-tasks/G01.01.md` |
| Baseline before change | `09` §6.1/§7, `11` §3/§5/§6/§12, `13` §4, `14` item 1, `15` P01, `16` coverage — all still stop-level `heard` (grep `heard: Set<stop_id>` hit all six) |
| Environment | Windows, Git Bash, Node v24.13.0, branch `zcode/13` from `origin/main` @ `21420a0`; no root `package.json` |

## Checks

| Check | Command / action | Expected | Actual |
|---|---|---|---|
| Executable model untouched and green | `node --test --test-reporter=spec docs/run-model/run-model.test.mjs` | all tests pass | 42 pass, 0 fail |
| Mutation gate | `node docs/run-model/check-regressions.mjs` | all reviewed regressions rejected | 12/12 rejected; «Repository model unchanged» |
| No competing field type in the six docs | grep `heard: Set<stop_id>` in `09/11/13/14/15/16` | only inside «Гісторыя:» clauses | 3 hits, all history-marked (09 §6.1, 13 §4, 14 item 1) |
| Story-level types present | grep `heard: Set<story_id>` | present in 09 §6.1, 11 §3.1, 11 §11 | present in all three |
| End list is story-unit | grep «адзінка» + `story_id` in 11 §3.1/§4.3 | story-unit wording with locked excluded | present |
| P01 status | `15` P01 heading/status | accepted, variant A, ADR + issue link | present |
| Coverage without losing IDs | `16` matrix | C1–C35 ranges kept, C36–C37 added | present; no renumbering (§15 N1–N7 note still valid) |
| Links | script over the six files resolving every markdown link target | no broken links | all resolve |
| Copy-paste gate | `npx --yes jscpd@5.1.2 --config .jscpd.json --no-tips .` | 0 clones | 0 clones |
| tsc / lint | — | N/A per task file: no root `package.json`; no dependencies installed for a docs review | not-run (not applicable) |
| OS / device behaviour | — | this is a documentation-contract part, no OS claim | not-run (out of scope; ADR §5 is a logic contract, not an OS proof) |

Revert probe (fails if the doc sync is reverted):

```bash
grep -q "heard: Set<story_id>" docs/architecture/09_technical_architecture.md \
  && grep -q "heard: Set<story_id>" docs/11_run_interaction.md \
  && grep -q "прынята (варыянт A, 2026-09-14)" docs/15_guide_decisions.md \
  && ! grep -q "загружаныя кропкі не ў" docs/11_run_interaction.md \
  && echo SYNC-OK
```

The last pattern is the old stop-unit wording of «Яшчэ можна адкрыць» and is absent from `11` now; the history note keeps a different wording («даступныя кропкі не ў heard», without «загружаныя») so it does not false-positive.

## Showboat decision

No demo: the diff is documentation-only (6 markdown files, +56/−43 by `git diff --numstat`), so none of the Showboat conditions 1–5 holds — no user-visible runtime behaviour, no code change, no contract change beyond syncing prose to the already-accepted ADR, no third runtime file. The executable proof of the underlying contract already exists (`docs/demos/2026-09-14-g01-01-b-story-progress.md`, G01.01.b).

## Handover — outside this part's allowed files

1. `docs/01_product_structure.md:123` still says «Яшчэ можна адкрыць» holds «даступныя загружаныя кропкі не ў heard» (stop-unit). File not in the allowed list for G01.01.c; the owner should sync it to the story-unit rule (ADR §4.6) in a separate change.
2. `docs/architecture/decisions/G01.03-session-access.md` lines 8, 31–33 say the G01.01 ADR is «яшчэ не прыняты» / `decision-required` and quote `09` as stop-level current law — now outdated. Its §3.8 conditional `heard` stop→story migration is **triggered** by the acceptance; G01.03.b should implement it as written there.
3. Parent result `docs/agent-tasks/results/G01.01.md` does not exist; per `agent-tasks/README.md` it is completed after the last part — with a/b/c done, the operator or the G01.01 epic owner can assemble it from the three part results.
4. `docs/plans/2026-09-13-discovery-feedback.md` line 21 («G01.01.a is still decision-required») is a dated planning snapshot; left as history.

## Review round — PR #43

Findings from CodeRabbit (3) and Cursor (`CHANGES_REQUESTED`, 5) on the PR; all accepted and fixed on this branch.

| Finding | Disposition |
|---|---|
| 11 §5.3: queue close when the primary story is already heard claimed the stop becomes `available` — contradicts the §3.1 precedence (`played` wins) | Accepted (HIGH, self-inflicted). §5.3 now adds `stop_id` to `auto_fired` and keeps the derived status `played`; the adjacent pre-existing «Калі праверка не прайшла» bullet had the same flaw and was fixed the same way |
| 09 §10: `session_ended` signature lacked the prose-required `reason` and the heard count | Accepted (HIGH; the prose/signature mismatch predates this PR, count unit changed by it). Signature now `session_ended(session_id, route_id, version, reason: user_stop | switch_walk, heard_stories_count)`; prose names the field. Exact analytics payload ownership stays with G01.05 |
| 09 invariant 7: `UserSelectedStory` missing from the `autoplay_suspended` clearing list | Accepted (MEDIUM). Added; 11 §5.2 now names both narration roles for manual play |
| 15: accepted P01 still sitting under «Прапанаваныя рашэнні — патрабуюць рашэння»; «Што рабіць наступным» still said «закрыць выбар гісторыі» | Accepted (MEDIUM). P01 moved to its own accepted section before the proposals header; next-steps reworded with the ADR link. P02/P03/P04 placement untouched (P04 already lived there with a status-marked heading; moving it is its owner's call) |
| 14:2026-09-13 line: accepted P01 status only in an italic footnote | Accepted (LOW). Sentence rewritten to past tense with the split named inline |
| LanguageTool (in the CodeRabbit comment): «умова 4 ўжо» → «ужо» after a digit | Accepted (trivial), fixed within the §5.3 rewrite |

No findings were rejected.

## Successor input

- Canonical vocabulary: `heard: Set<story_id>`, `auto_fired: Set<stop_id>`, `playing { stop_id, story_id, play_id }`, `queued { stop_id, radius, at }`, derived `primary_story_id` / `additional_story_id`, `story_accessible`, `UserSelectedStory(stop_id, story_id)`; markers are stop-unit from the primary; «Яшчэ можна адкрыць» is story-unit, locked excluded.
- G01.02 (P02) works on top of this vocabulary; its `agent:running` issue #14 predates the acceptance and should re-check §4.3's manual-play rules against its owner contract.
- G01.03.b implements the durable schema including the §3.8 conditional migration now triggered by variant A.
- `11` criteria for device acceptance now include C36–C37; coverage lives in `16`.
