# G18.04 result — size guideline in the review rules

**Status:** `complete`

Encodes the size orientation (file ≲ 400 lines / contract ≲ 12 public methods → the
reviewer writes one phrase asking whether a second owner or a contract wants out) as a
review-time obligation in `docs/agent-rules/code-review.md`, worded as orientation and
explicitly not a gate. Guarded by `tools/validate/size-guideline-guard.test.mjs`.
Issue [#167](https://github.com/vokhandcrafts/KUDY/issues/167), branch `zcode/167`.

## What shipped

1. **`docs/agent-rules/code-review.md`** — new §4 subsection «Size orientation — one
   phrase, not dogma» (5 lines, English per documentation-language): the numbers are
   orientation, no CI check enforces them, nothing is refactored for the count's sake,
   assessment happens on the next real touch. Placed between «Tests» and «Recurring
   corpus classes» — the reviewer reads it with the rest of the §4 checklist.
2. **`tools/validate/size-guideline-guard.test.mjs`** — 4 tests: the real rules pass;
   missing file fails; removed section fails; lost anchor fails (three anchor mutations:
   `12 public methods`, `400`, `orientation, not a gate`). The guard also rejects a
   duplicated section. Negative mutations run on temp copies; `mkdtemp` cleaned in
   `finally`.
3. **No second copy** — grep before the change found no size rule in `implementation-rules.md`,
   `lessons-learned.md` or any architecture doc (`400` there is only the HTTP
   `invalid_request` code, unrelated); the guideline now lives in `code-review.md` only,
   and the guard file's header comment pins that single home.

## Acceptance → evidence (issue #167, item by item)

1. **One-phrase requirement with ≤400/≤12 orientation, not dogma** — the section names
   both numbers, requires one reviewer phrase («is there a second owner or a contract
   asking to be extracted?») and states the orientation/non-gate wording verbatim.
2. **Guideline lives in code-review.md only** — duplication grep clean (above); the
   guard comment records the single-home rule; nothing added to other agent-rules files
   (git diff shows only `code-review.md` + the new test + this results file).
3. **Guard fails if the guideline is removed** — `node --test tools/validate/size-guideline-guard.test.mjs`
   4/4 pass; the section-removal and anchor-loss mutations make it throw; weakening the
   guard makes its own negative tests fail (revert-failing).
4. **`tools/build-bundle/build-bundle.mjs` not touched** — `git diff origin/main...zcode/167 --stat`
   lists exactly three files, build-bundle not among them; the file stands at 680 lines
   (audit figure), to be assessed on its next real touch.

## Proof

```
node --test tools/validate/size-guideline-guard.test.mjs
```

4/4 pass; removing the guideline section from `code-review.md` (or losing any of its
anchors) fails the default `npm test` run.

## Gates at final HEAD (this session)

- `node --test tools/validate/size-guideline-guard.test.mjs` — 4/4 pass.
- `npm test` — 359 pass / 0 fail on this branch (main baseline 355 + 4 new guard tests;
  the 9 vocabulary-guard tests of PR #170 are not on this branch's base).
- `npm run typecheck` — clean (no TS in the diff).
- jscpd 5.1.2 (`--config .jscpd.json`) — 0 clones.
- Mixed-script grep over the changed rules text — clean (English text, no Cyrillic).

## Out of scope kept

No refactor of `build-bundle.mjs` (explicitly forbidden by the plan); no hard limits or
CI gates on file size; no other agent-rules files touched.
