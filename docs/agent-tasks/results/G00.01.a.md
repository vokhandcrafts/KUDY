# G00.01.a result — repeatable GPS/audio example

**Status:** `blocked-external` (code-ready core; native build and real-device acceptance are not verified).

## Result

The isolated spike contains an Expo development-build shell, three generated legal synthetic WAV tones with Belarusian transcripts, a deterministic location/audio session model, and eight passing Node behavior tests. The model enforces one location owner and one player, Start readiness, version pinning, GPS release on Pause/End or permission/subscription failure, explicit Resume, stale/poor-fix rejection, dwell, manual playback, interruption suspension, and consent-independent local progress.

No claim is made that background GPS/audio works on an OS. No coordinates were sent, displayed, or placed in diagnostics.

## Exact changed files

- `spikes/G00.01-location-audio/App.tsx`
- `spikes/G00.01-location-audio/README.md`
- `spikes/G00.01-location-audio/VERSIONS.lock.json`
- `spikes/G00.01-location-audio/app.json`
- `spikes/G00.01-location-audio/assets/crane.wav`
- `spikes/G00.01-location-audio/assets/gate.wav`
- `spikes/G00.01-location-audio/assets/fountain.wav`
- `spikes/G00.01-location-audio/eas.json`
- `spikes/G00.01-location-audio/index.js`
- `spikes/G00.01-location-audio/package.json`
- `spikes/G00.01-location-audio/scripts/generate-audio.mjs`
- `spikes/G00.01-location-audio/src/session.d.ts`
- `spikes/G00.01-location-audio/src/session.mjs`
- `spikes/G00.01-location-audio/test/session.test.mjs`
- `spikes/G00.01-location-audio/tsconfig.json`
- `docs/agent-tasks/results/G00.01.a.md`

No predecessor results were required; the parent brief declares no dependencies. The spike directory, atomic result, and root `package.json` were absent before work.

## Inputs and versions

- Observed locally: Linux checkout, Node `v22.23.2`, npm `10.9.8`.
- Missing locally: Java/JDK, Android SDK/ADB, Xcode/iOS build path, Expo dependencies, `uvx`.
- Candidate direct versions are recorded in `package.json` and `VERSIONS.lock.json`; they are not reported as installed or officially verified because the issue forbade new network access and npm had no cached metadata.
- `npm install --package-lock-only --ignore-scripts --offline` failed with `ENOTCACHED` for `@types/react`. Consequently there is no fabricated `package-lock.json`; `VERSIONS.lock.json` explicitly is not a transitive package-manager lock.

## Checks: expected and actual

| Command/action | Expected | Actual / evidence |
|---|---|---|
| Initial `npm test` | New behavior test fails before implementation | Failed with `ERR_MODULE_NOT_FOUND` for `src/session.mjs`. |
| `npm run generate-audio` | Create three deterministic local WAV assets | Passed; created `crane.wav`, `gate.wav`, and `fountain.wav`. |
| `npm test` after implementation (from spike directory) | Eight contract scenarios pass | Passed: 8 tests, 0 failures. A separate root-directory invocation failed as expected because the checkout has no root `package.json`; it is not counted as a spike failure. |
| `npm install --package-lock-only --ignore-scripts --offline` | Generate npm lock without network if cache is complete | Failed: `ENOTCACHED`; no package metadata cached. |
| `node --test --test-reporter=spec docs/run-model/run-model.test.mjs` | Existing model stays green | Passed: 32 tests, 0 failures. |
| `node docs/run-model/check-regressions.mjs` | No model regressions | Passed: 9/9 reviewed regressions rejected; repository model unchanged. |
| WAV header inspection with Node | Three deterministic RIFF/WAVE, 8 kHz files | Passed: each file is 5,644 bytes and reports an 8,000 Hz header. SHA-256 values: crane `e431cd5d…e14d8f1`, gate `a95a8374…130ced`, fountain `82eb739a…df981`. |
| Android/iOS development build | Compile and launch the native spike | `not-run`: no dependencies, JDK/Android SDK/ADB, Xcode, or permitted downloads. |
| Real-device matrix in README | Record actual OS behavior and delays | `not-run`: no connected iPhone/Android devices, including Samsung/Xiaomi. |
| `uvx showboat==0.6.1 verify ...` | Verify local executable proof | `not-run`: `uvx` is unavailable and downloads are forbidden; per policy no demo file was fabricated. |
| TypeScript / lint | Compile and lint installed app | `not-run`: dependencies unavailable; no lint script is defined. |

## Unfinished and blocker

Full closure requires permitted access to official Expo documentation/package metadata, a generated npm lockfile, an installed development build toolchain, and the real iPhone/Android device matrix. A mock or Node test cannot close those platform cells. This status must remain `blocked-external` until those facts exist.

## Exact successor input

From `spikes/G00.01-location-audio/`, verify compatible current Expo/location/audio versions using the official pages linked in README, generate `package-lock.json`, run `npm install`, `npx expo-doctor`, native development builds, and every README device-matrix row on a real iPhone, clean Android, and Samsung/Xiaomi. Append actual device/OS/build identifiers, permission states, timing/battery measurements, and redacted local evidence here; only then reconsider `complete`. Do not start G00.01.b as part of that work.
