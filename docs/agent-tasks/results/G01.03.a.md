# G01.03.a result — Durable session and access contract

**Status:** `decision-required`

The named deliverable exists: `docs/architecture/decisions/G01.03-session-access.md`. It is a concrete ADR awaiting two recorded choices: the founder's A/B decision on P01 (variant A changes the `heard` element type and drives its migration, §3.8 of the ADR), and reconciliation with the G01.02.c persisted/transient field hand-off, which does not exist yet (issue #16 open, no result file). Per the closing conditions, `complete` is forbidden while an unapproved dependent product choice remains.

**Dependency deviation (recorded, operator-authorized):** this issue is `Blocked-by: #16` (G01.02.c), which was open when work started. The operator assigned this issue explicitly in chat and pre-authorized proceeding; the claim comment (issue #17, comment 5656984077) records this. Nothing from G01.02.c was invented as accepted: field identities derive only from already-accepted sources (`09` §6.1/§7, `11` разд. 3, R-rules, the G01.01.a ADR as a clearly-marked proposal).

## Changed files

- `docs/architecture/decisions/G01.03-session-access.md` (created; 217 lines)
- `docs/agent-tasks/results/G01.03.a.md` (this file)

Not changed: `09`, `11`, `15`, `14`, `16`, `18`, `run-model/*`, parent `docs/agent-tasks/results/G01.03.md`, any G01.02-owned file. No demo, no merge, no push to stores, no real purchases, no secrets, no new network access beyond GitHub API calls for the issue/PR protocol.

## Inputs

| Input | Fact |
|---|---|
| Predecessor results | `results/G01.01.a.md` present, status `decision-required`; no G01.02.* results |
| `docs/architecture/decisions/G01.01-narration-progress.md` | identities `stop_id`, `story_id`, `session_id`, `play_id`; `heard` proposed `Set<story_id>` under variant A; durable SQLite/migration of `heard[]` explicitly deferred to G01.03 |
| `docs/architecture/09_technical_architecture.md` §6.1, §7 (current law) | `heard: Set<stop_id>`; session table shape; zone A/B rules; `AccessReady(version, stop_ids[])` accepted only from the download service after grant + full verified load, same version, known stop_ids |
| `docs/11_run_interaction.md` разд. 3, 4, 7, 8 | session states; one live session; restore offers continue; upgrade keeps progress; version pinned till finish |
| `docs/architecture/18_component_blueprint.md` §2, §4 | owner map: `services/db` transactions, `services/download` issues trusted AccessReady; effects only after DB commit on Start |
| `docs/architecture/21_discovery_feedback_architecture.md` | `feedback_local`/`feedback_outbox` are separate zone-B tables; `discovery_cache` zone A; additive migrations without touching session |
| `docs/15_guide_decisions.md` | R01–R10 accepted; P01/P02 proposals; R07 durable session-scope shown/dismissed, G07.04 owns cooldown values |
| Environment | Windows (win32, Git Bash), Node v24.13.0, no root `package.json`; workspace is a git checkout on branch `zcode/17` off `origin/main` (c22a02f) |

## Checks

| Action | Expected | Actual | Evidence |
|---|---|---|---|
| ADR path exists with named deliverable | `docs/architecture/decisions/G01.03-session-access.md`, header status `decision-required` | exists; status table row 1 = `decision-required` | file header |
| Field table: name/type/nullable/default/owner + write & restore moment | §3.1 table for `session_id`, `route_id`, `version`, `locale`, `tier`, `state`, `started_at`, `finished_at`, `auto_fired`, `heard`, `last_stop_id`, `play_seq`; §3.2 derived fields with restore sources | present; `play_seq` added as a new durable field with write-through rule (late-callback identity survives restart) | ADR §3.1–§3.2 |
| Transactional Start/switch/End; max one live session; new `session_id` per repeat | §3.3 table; partial unique index; switch rolls back both rows; restore never starts audio | present, incl. SQL for the unique index and effects-after-commit rule | ADR §3.3 |
| Version immutable | §3.4: pinned at Start till End incl. pause/restart/next-day/catalog update/rollback | present | ADR §3.4 |
| AccessReady trust boundary includes route/version/locale/tier and download owner | §3.5 six acceptance conditions + issuer; late download rejected; repeat idempotent | present; explicit note that this refines `09`'s `AccessReady(version, stop_ids[])` and the sync belongs to G01.03.c | ADR §3.5 |
| Entitlement vs files vs narrative vs listening separated; purchased-without-files not ready; no ready bool | §3.6 four-row table; no ready column in zone B; retry download without re-pay | present | ADR §3.6 |
| Crash near activation/persist, staging, missing file after restart, failed upgrade keeps old layer | §3.7 table | present | ADR §3.7 |
| Migration contract; failure never wipes DB | §3.8: `user_version`, one-step transactions, zone-B drop forbidden, conditional `heard` stop→story migration with `migration_log` for unresolved ids | present | ADR §3.8 |
| R07 shown/dismissed location + Start transfer; cooldown numbers owned by G07.04 | §3.9 two tables + transfer rule; values delegated to G07.04 | present | ADR §3.9 |
| Session lifetime vs server retention | §3.10; no new retention term invented (G11.03 owns it) | present | ADR §3.10 |
| Scenario coverage | all nine parent scenarios mapped | §4 table covers all nine | ADR §4 |
| No competing normative rules | ADR marked as gaining force only after acceptance; `09`/`11` remain law until then | header table "Сіла гэтага файла" + §3 preamble | ADR header, §3 |
| Field names vs accepted ADR G01.01 | reuses `session_id`, `story_id`, `stop_id`, `play_id`, `auto_fired`, `heard`, `tier_available`, `story_accessible` as named there; introduces only new session-owned fields (`play_seq`, `guide_hint_state`, `guide_hint_last`, `migration_log`) | no renames of G01.01-owned names; `18` §2 assigns durable-field contracts to G01.03.a | ADR §3.1–§3.2, §3.9 |
| Relative links resolve | all linked files exist in checkout | 9/9 OK | shell loop over link targets |
| `git diff --numstat` scope | only the two allowed outputs | `217  0  docs/architecture/decisions/G01.03-session-access.md` (+ this results file) | git |
| `npx tsc --noEmit` / `npm run lint` | N/A without root `package.json`; do not install deps for a docs review | not run; `test -f package.json` → `NO_PKG` | shell |
| jscpd copy-paste gate | 0 clones | `Found 0 clones.` (12 files, 1028 lines analyzed) | npx jscpd@5.1.2 output |
| Showboat decision rule | docs-only diff: no runtime files/lines, none of conditions 1–5 → no demo | not created; conditions checked explicitly | `docs/agent-rules/showboat.md` |
| OS / store / server / SQLite proof | not claimed | not run; marked as G04/G05/G08 + spikes | ADR §4 closing note |

## Code review (own diff)

Scope: new ADR markdown + this results file. Lead concern: contract conformance.

Pre-checks: `git diff --numstat` (2 docs files, 0 runtime); `git log` clean of merges/secrets; no `package.json` → tsc/lint N/A; no new dependencies; no secrets; §17 forbidden list N/A (no runtime code introduced).

Findings checked by hand: zone-A/B boundary respected (no ready flag in zone B, cache purge cannot touch `session`); layering respected (transactions owned by `services/db`, single writer `useRunController`, engine pure); no coordinates in analytics (R07 rows store guide_id + timestamps only); no client-side entitlement store introduced; migration is non-destructive and reversible-by-report; the `AccessReady` identity refinement over `09` is stated as a refinement, not a silent norm change.

```
Summary: 0 critical · 0 high · 0 medium · 0 low
Verdict: not mergeable as product law — decision-required status; document is ready for the founder's choices
```

## Not done

- Founder choice on P01 variant A (ADR G01.01) — blocks `complete` here and blocks G01.03.b modeling.
- Reconciliation with G01.02.c persisted/transient field list (issue #16 open).
- `run-model` scenarios — G01.03.b after accepted ADR.
- Sync of `09`/`11`/`15`/`14`/`16`/`18` — G01.03.c; parent result `docs/agent-tasks/results/G01.03.md` only after all parts.
- Showboat demo — not required by policy.
- Production SQLite/migration/download code — G04/G05/G08.

## Successor input (G01.03.b)

1. Read the ADR header first. While status is not accepted, do not encode §3 into `run-model`.
2. After acceptance, model §3.3 transaction boundaries, §3.5 AccessReady rejection conditions (all six), §3.7 crash table, and the §4 scenario answers; reuse field names from §3.1–§3.2 verbatim; keep `AccessReady` rejection wholly-atomic (no partial mutation).
3. Model inputs already pinned: `play_seq` write-through before audio commands; restore defaults `playing=null`, `queued=null`, `autoplay_suspended=true`; monotone `heard`/`auto_fired`.
4. Allowed files for G01.03.b: `docs/run-model/*` plus its own result/demo only.
