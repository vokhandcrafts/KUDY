# G00.03.c — Рэальныя sandbox purchase і restore

**Дата:** 2026-09-15
**Статус:** `blocked-external` — названы вынік (рэальныя sandbox purchase і
restore з рэдактаванымі доказамі) не выкананы: у асяроддзі няма App Store
Connect / Google Play sandbox-акаўнтаў, праекта RevenueCat, фізічных прылад і
сеткавага доступу (фіксацыя .a і .b). Падрыхтаванае ўсё, што task-файл дазваляе
без прылад: выкананы вербатыўна пратакол рэальнага прагону ў
`spikes/G00.03-sandbox-grant/evidence/README.md`. Спайк застаецца зялёным; ніякі
mock не выдаецца за store-доказ.

## Зыходны стан і план

- Залежнасці: .a `complete` (спайк на кананічным кантракце 09 §5, 20/20),
  .b `complete` (негатыўны набор 18 pass / 6 skip у mock, 6/6 live супраць
  лакальнага сервера; мутат-довад аховаў). Абодва фактычна пераздадзеныя пры
  гэтай працы (гл. «Праверкі і доказы»).
- Тэчка `spikes/G00.03-sandbox-grant/evidence/` не існавала; result
  `G00.03.c.md` не існаваў; код .a/.b не мяняўся.
- План: (1) пацвердзіць інвентарызацыю доступаў; (2) пісьмовы пратакол
  дзвюх крам: purchase→server grant→байты, restore на чыстай ўстаноўцы,
  webhook выключаны, рэдактаванне доказаў; (3) прагнаць фактычныя брамы
  спайку; (4) self-review; (5) result са статусам і дакладным уваходам для
  G00.03.d.

## Вынік

Створаны `spikes/G00.03-sandbox-grant/evidence/README.md` — выкананы
вербатыўна пратакол рэальнага прагону: прадумовы (акаўнты, трэк, RC-праект,
env сервера, https-base кліента, webhook выключаны), прагон iOS
(purchase→grant→байты, паўтор з кэшам), прагон Android (internal testing),
restore на чыстай ўстаноўцы без другога плацяжу, негатывы без права на
прыладзе (чаканы 403 `no_entitlement`, кліенцкі сцяг не дасылаецца), фармат
і naming рэдактаваных доказаў (што забаронена ў evidence: сакрэты, bearer,
поўныя URL, receipts, асабістыя даныя), чэкліст крытэраў закрыцця .c.

Пратакол звяраны з фактычнымі магчымасцямі спайку: назвы env — з
`.env.example`, серверныя каманды — з README спайку, негатывы — з праверанымі
.b паводзінамі сервера; асобна адзначана, што серверны лічильнік плацяжоў у
спайку не мадэляваўся, таму доказ «без другога плацяжу» пры restore бярэм з
крамы/RC UI, а не з логаў спайку.

## Змененыя файлы

- `spikes/G00.03-sandbox-grant/evidence/README.md` (новы)
- `docs/agent-tasks/results/G00.03.c.md` (гэты файл)

Код .a/.b, README спайку, `.env.example`, `server/catalog.json` — не мяняліся.

## Прынятыя ўваходы

| Уваход | Факт на 2026-09-15 |
|---|---|
| Result G00.03.a / G00.03.b | абодва complete; доказы пераздадзеныя пры гэтай праце |
| Task-файл `atomic/G00.03.c.md` | вынік, дазволеная тэчка evidence/, умова «няма store/account — blocked-external» |
| `.env.example`, README спайку | назвы env, крокі store-канфігурацыі 1–5, забарона сеткі/production |
| Асяроддзе | Node 24.13.0, npm 11.6.2; крамы/прылады/RC-праект/сетка — недаступныя |

## Праверкі і доказы

1. `cd spikes/G00.03-sandbox-grant && npm test` (набор .a)
   - Чакалася: 20/20; спайк не пашкоджаны.
   - Фактычна: **20/20 pass**.
2. `cd spikes/G00.03-sandbox-grant && node --test --test-reporter=spec tests/*.test.mjs` (набор .b, mock)
   - Чакалася: 18 pass / 6 live-skip; у тым лічбе мутат-довад аховаў.
   - Фактычна: **tests 24 · pass 18 · skipped 6 · fail 0**.
3. `npx --yes jscpd@5.1.2 --config .jscpd.json --no-tips .` з кораня
   - Чакалася: 0 clones.
   - Фактычна: **Found 0 clones** (0.00%).
4. tsc/lint: непрыменныя — root `package.json` няма; новых залежнасцей для
   review не ўсталёўвалася (забарона новага сеткавага доступу).
5. Showboat: **не ўзнікае** — дыф гэтай працы толькі дакументы (пратакол +
   result): 0 runtime source файлаў/радкоў, умовы 1–4 decision rule змяненнем
   не выкліканыя; акрамя таго, `uvx` у асяроддзі недаступны (фіксацыя .a).

## Not-run і блокеры (дакладны спіс)

- **Purchase → server grant → байты на iOS (App Store Connect sandbox)** —
  `not-run`: няма акаўнта/прылады/RC-праекта; крокі — evidence/README, Прагон 1.
- **Purchase → server grant → байты на Android (internal testing)** —
  `not-run`: тая ж прычына; крокі — Прагон 2.
- **Restore на чыстай ўстаноўцы без другога плацяжу** — `not-run`: Прагон 3.
- **Негатывы без права на прыладзе (403, нуль байтаў)** — `not-run`:
  раздзел «Негатывы» пратаколу; серверны бок гэтых адмоў пакрыты mock-наборам .b.
- **Пацверджанне формы адказу RevenueCat REST** (у тым лічбе 404 і
  `product_identifier`) — `not-run`: забарона новага сеткавага доступу;
  адаптар .a да жывой звяркі паводзіць fail-closed.
- **Webhook выключаны (факт канфігурацыі RC)** — `not-run`: правяраецца ў
  RC-кансолі пры прагоне; сервер спайку webhook-ручніка не мае па дызайне.

## Што можа браць наступнік (G00.03.d)

- .d чакае рэалізацыі .c: бацькоўскі result `G00.03.md` дапаўняецца пасля
  апошняй часткі (правіла task-файла), сёння не створаны — свядома.
- Выканаўцу з доступамі: крокі — `evidence/README.md` (вербатыўна), naming
  evidence-файлаў і правілы рэдактавання — там жа; пасля прагону перавесці
  result .c у `complete` і дадаць спасылкі на evidence.
- Форма RC REST і sandbox/production-атрыбуцыя — адкрытыя кропкі з .a/.b;
  пры прагоне фіксаваць як факты, не як тлумачэнні.
- Вядомае адкрытае мярджанае ўдасканаленне спайку (не блакер .c): follow-up
  PR да набору .b — asserts `Retry-After` на 503 і пашыранае live-пакрыццё
  (на момант гэтага result — адкрыты PR, не ў main).
