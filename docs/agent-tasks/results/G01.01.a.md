# G01.01.a result — Narration and progress contract

**Status:** `decision-required`

P01 is still a proposal in `docs/15_guide_decisions.md`. This part produced a concrete ADR and a recommendation (variant A). It does not record A as accepted. G01.01.b must not implement the model until the founder writes the choice on the ADR.

## Changed files

- `docs/architecture/decisions/G01.01-narration-progress.md` (created)
- `docs/agent-tasks/results/G01.01.a.md` (this file)

Not changed: `09`, `11`, `13`, `14`, `15`, `16`, `run-model/*`, parent `docs/agent-tasks/results/G01.01.md`. No demo. No merge, push, or network.

## Inputs

| Input | Fact |
|---|---|
| Predecessor results | none; `docs/agent-tasks/results/` was empty; G01.01.a has no dependency |
| `docs/15_guide_decisions.md` | R01–R07 accepted; P01 status «прапанова, не рэалізавана» |
| `docs/architecture/09_technical_architecture.md` §3, §6.1, §7 | `heard: Set<stop_id>`, `auto_fired: Set<stop_id>`, `playing?: { stop_id, story_id, play_id }`, `RouteStop.story_base_id` / `story_extended_id` |
| `docs/11_run_interaction.md` §3, §5, §6, §12 | stop-level heard; «Яшчэ можна адкрыць» = accessible stops not in heard; C31–C35 |
| `docs/13_first_walk.md` §4 | open: base heard must not mean extended heard |
| `docs/architecture/18_component_blueprint.md` | PROGRESS stop/story semantics are this task’s output |
| `docs/run-model/run-model.mjs` | `heard: []` of stop ids; `status`/`missed`/`eligible` key on stop id |
| Environment | Linux WSL2, Node present, no root `package.json`, workspace is not a git repo |

Baseline: `docs/architecture/decisions/` did not exist; current normative progress is one narrative per stop.

Plan executed: write only the named ADR; keep §4 labelled proposed; do not sync docs or the model.

## Checks

| Action | Expected | Actual | Evidence |
|---|---|---|---|
| ADR path exists | `docs/architecture/decisions/G01.01-narration-progress.md` | exists; decision status `decision-required`; «Прыняты выбар \| няма» | file header |
| Compare P01 vs replace-primary | both options, cost table, recommendation not marked accepted | variants A/B in §2–§3; line «Гэта не прыняты выбар» | ADR §2–§3 |
| Fields: type, owner, change rule | table for session/version/locale/auto_fired/heard/playing/queued/access/tier | present; `heard` proposed `Set<story_id>`; `auto_fired` stays `Set<stop_id>` | ADR §4.2 |
| Primary / additional / paid-only / markers / «Яшчэ можна адкрыць» | primary derived from base else extended; no fake free story; map unit = stop; list unit = story; locked excluded | §4.1, §4.3–§4.6 | ADR |
| Parent scenarios | seven rows from `docs/agent-tasks/G01.01.md` | §5 table covers all seven, including base→unlock with no Play | ADR §5 |
| Unique names vs competing norms | ADR does not rewrite `09`/`11`/`run-model`; those still say `heard: Set<stop_id>` as current law | `rg 'heard: Set<stop_id>'` hits `09`, `11`, `13`, `14`, parent brief — not treated as updated; ADR forbids using that type after acceptance A | workspace grep 2026-09-08 |
| Out of scope files | unchanged | only the two allowed outputs created | find on decisions/results; no run-model edits |
| `npx tsc --noEmit` / `npm run lint` | N/A without package.json | not run | `test -f /workspace/package.json` → `NO_PKG` |
| Showboat | policy: docs-only, none of conditions 1–5 | not created | `docs/agent-rules/showboat.md` |
| OS / store / server | not claimed | not run | — |

## Code review (own diff)

Scope: new ADR markdown only. Lead concern: contract conformance.

Pre-checks: no git history; tsc/lint N/A; no secrets; no new dependencies; architecture §17 forbidden list N/A (no runtime).

```
Summary: 0 critical · 0 high · 0 medium · 0 low
Verdict: not mergeable as product law — waiting for founder A/B; document is ready for that choice
```

No competing *new* normative file: until acceptance, `09`/`11`/`run-model` remain the one-narrative rules; the ADR says so in the header.

## Not done

- Founder choice not recorded (`complete` forbidden while P01 is unapproved).
- `run-model` tests, mutation that credits extended from base — G01.01.b after accepted ADR.
- Sync of `09`/`11`/`13`/`14`/`15`/`16` — G01.01.c.
- Parent result `docs/agent-tasks/results/G01.01.md` — only after all G01.01 parts are `complete`.
- Showboat demo — not required.

## Successor input (G01.01.b)

1. Read `docs/architecture/decisions/G01.01-narration-progress.md` header. If status is still waiting, **stop**; do not encode §4 into the model.
2. If the founder marked **variant A accepted**, implement only that ADR: `heard` keyed by `story_id`; `auto_fired` by `stop_id`; derived `primary_story_id` / `additional_story_id`; `story_accessible` from `accessible_stop_ids` + `tier_available`; `UserSelectedStory`; no Play from AccessReady; failing test first for base-heard → extended-heard.
3. If the founder marked **variant B**, do not implement §4; return the ADR to G01.01.a with a reproduction that A is not the choice.
4. Allowed files for G01.01.b: `docs/run-model/*` plus its own result/demo only.
