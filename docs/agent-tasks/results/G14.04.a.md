# G14.04.a — Ukrainian localization release scope

**Date:** 2026-09-15
**Status:** `decision-required` — the release package is fully drafted (scope, honesty rules, review ownership, child tasks); the founder decisions listed in §6 of the package are unanswered, so no child task is started and nothing is published. No fabricated release date, narrator or schedule.

## Starting state and plan

- The brief existed in `docs/agent-tasks/discovery/` on `main`; no result file, no branch, no issue existed for G14.04.a (the discovery README records G14.04.a as not yet published to the issue board). No other agent's working files were present in the allowed paths.
- Inventory first: the whole checkout was searched for locale-bearing files, catalogs, `voices.json`, UI string files. Findings: exactly one synthetic `be` transcript fixture in the G00.03 spike; zero `en`/`uk` content anywhere; zero real audio; no app code (G00.04/G06 own the scaffold). The `authoring_locale=be` / `fallback_locale=en` decisions and the `en→pl→de→es` queue are unchanged law.
- Plan: one human-facing package (`docs/localization/uk-release-scope.md`, Belarusian), narrow edits to `04` (localization decisions only) and `16` (G14.04 child rows only), this result. No implementation, no translation.

## Result

1. **[docs/localization/uk-release-scope.md](../localization/uk-release-scope.md)** — the reviewable package:
   - factual inventory of BE/EN/UK UI, text, audio and review resources (§2), with "planned ≠ available" marked per row;
   - one coherent Ukrainian experience proposal: UK UI strings, one fully translated guide (point texts, cards, transcripts, that guide's Moments), discovery cards with `content_locale: uk`, feedback form/report semantics, transcript reading, and audio as a separate readiness unit with its own date (§3);
   - honesty rules bound to existing law (`21` §8, L01/L02, `20` §10, `09` §8): separate `ui_locales`/`text_locales`/`audio_locales`; UK text never claims UK audio and never silently substitutes be/ru; UI language never changes an active Run; no second purchase for a language; BE start and EN fallback preserved (§4);
   - review ownership: a native-speaker reviewer is mandatory and is currently **unavailable/unassigned** — recorded as a content blocker, not invented; translation = new Draft through the unchanged G03.01–G03.04 claims/review process, with name-pronunciation and memorial-tone acceptance criteria (§5);
   - five concrete founder decisions (queue position vs `pl`, first-package scope, UI readiness timing, reviewer resource, no-cost-of-language confirmation) — §6, `decision-required`;
   - five child tasks with stable IDs mapped to existing epic owners, none started: **G14.04.b** (G02.01–G02.04: `uk` as planned locale in bundle/catalog contracts), **G14.04.c** (G03.01–G03.04: first guide translation), **G14.04.d** (G06/G10: UK UI strings), **G14.04.e** (G03.03 line: UK audio recording), **G14.04.f** (G11.01/G14: L01/L02 and expanded-MVP acceptance) — §7.
2. **[docs/04_scope_and_roadmap.md](../04_scope_and_roadmap.md)** — localization decisions only: the two open-question bullets now point to the package and its `decision-required` status; the expansion note at the end references the package. No other roadmap content touched.
3. **[docs/16_delivery_backlog.md](../16_delivery_backlog.md)** — G14.04 child tasks only: the G14.04.a note now lists the child table G14.04.b–f with owner epics and dependencies. No other backlog rows changed.

## Exact changed files

- `docs/localization/uk-release-scope.md` (new)
- `docs/04_scope_and_roadmap.md` (3 localization lines)
- `docs/16_delivery_backlog.md` (G14.04 child table)
- `docs/agent-tasks/results/G14.04.a.md` (this file)

## Inputs and versions

| Input | Fact on 2026-09-15 |
|---|---|
| `20` §10, `21` §8–§9 (UK row), `04`, `09` §8, R10, plan 2026-09-13 §2/§6/§8, `16` G14.04 | read in full; rules consumed, not changed |
| Repository inventory | 1 synthetic `be` fixture (G00.03 spike); no `en`/`uk` content, audio, `voices.json` or UI string files |
| Acceptance rows consumed | L01–L02 (`20` §11), plan §8 coverage row `L01–L02 → G02/G06/G14.04.a` |

## Checks

1. Inventory re-runnable command (expected: only the listed synthetic fixture):
   `find . -path ./.git -prune -o -type f \( -name "*.be.*" -o -name "*.en.*" -o -name "*.uk.*" \) -print`
   Actual: exactly `spikes/G00.03-sandbox-grant/data/storage/.../transcripts/private-story-01.be.txt`. No `voices.json`/strings/`.arb` files exist (`find … -name "voices.json" -o -name "*strings*" -o -name "*.arb"` → empty).
2. Consistency against accepted law (manual cross-check): the package repeats `09` §8 language chain, `21` §8 locale triples and Start/access rules, `20` §10 review rule, L01/L02 wording without paraphrasing them into competing norms; BE/EN decisions marked unchanged; `04`/`16` edits reference the package instead of restating rules.
3. Acceptance walkthrough (task list 1–5): separate readiness entries — §2/§3; text-only-UK honesty — §4 (L01/L02); BE start + EN fallback explicit — §4; no second purchase for language — §4/§6.5; real decisions and unavailable resources recorded, no dates — §2/§5/§6. All five hold.
4. `npx --yes jscpd@5.1.2 --config .jscpd.json --no-tips .` from root — expected 0 clones; **actual: Found 0 clones** (0.00%).
5. tsc/lint: not applicable — documentation-only task, no `package.json` at root; no toolchain installed (plan §4: do not install a stack to validate Markdown).
6. Showboat: not triggered — the diff adds/plans no runtime behavior, fixes no bug, touches no contract implementation; it is a planning/contract document like the other contract tasks. No demo created.

## Not-run and limitations

- Everything downstream is intentionally not started: no translation, no UI strings, no schema changes, no audio, no publication. G14.04 parent remains not complete.
- The native-speaker reviewer and any narrator are **unavailable resources** recorded as such; no substitute was assumed.
- GitHub issue for G14.04.a does not exist (discovery README §"Публікацыя ў GitHub" records it as unpublished; the earlier publication run stopped at a usage limit). This executor has no GitHub write access (no `gh` CLI, browser session logged out, token reading forbidden by `dispatcher-loop.md`), so no issue was created, no status label was set, and no PR was opened — the branch is pushed for the operator/dispatcher instead.

## Handoff to the next agent

- The next eligible work is any operator-assigned open issue; wave-2 G15/G16 tasks remain dependency-blocked (G02/G03/G08/G09), G14.04.b–f wait on the founder decisions in package §6. The first child to run after acceptance is G14.04.b (schema-level, owner G02).
- If the founder answers §6, draft full briefs for G14.04.b–f (briefs are English, per the discovery README conventions) before starting them.
- When GitHub access is restored, publish the missing wave-2 issues (check duplicates first, per the discovery README) and link G14.04.b–f to the parent epic.
