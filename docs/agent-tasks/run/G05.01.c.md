# G05.01.c — Audio ownership in the reducer (P02)

**Status:** not-started. **Model class:** strong. **Dependencies:** [G05.01.b](G05.01.b.md) complete. Parent task: G05.01.

## Scope

The G01.02 owner rules as reducer transitions: guide and moment variants of `playing`, tagged callbacks, focus loss and the explicit return to the guide. No real player — that is G05.03.

Read: [ADR G01.02](../../architecture/decisions/G01.02-audio-ownership.md) §3.1–§3.8; [09 §6.1](../../architecture/09_technical_architecture.md) (`PlayMoment`, `MomentFinished`, `AudioFailed`, `UserStoppedAudio`, `ResumeAudio`, `GuideResume`, `FocusLoss`/`FocusRegain`; invariants 5–7); [11](../../11_run_interaction.md) criteria C18–C20, C38–C45; [run-model README](../../run-model/README.md) row G01.02.b.

## Allowed outputs

- `core/engine/` — reducer and tests
- `docs/demos/<YYYY-MM-DD>-g0501c-audio-ownership.md`
- `docs/agent-tasks/results/G05.01.c.md`

## Acceptance

1. [ ] `playing` has guide | moment variants and the token is `{kind, ref, seq}`; a callback with a non-matching token is ignored entirely (no heard, no stop, no queue start), with one test per callback kind.
2. [ ] An explicit `PlayMoment` stops the guide by command (not as finished), clears the queue and suspends automation until `GuideResume`; `MomentFinished` never mutates `heard`.
3. [ ] `FocusLoss` is never `AudioFinished`; `FocusRegain` more than 10 min after `focus_lost_at` closes the play, within 10 min `ResumeAudio` continues the same token; a manual pause has no threshold.
4. [ ] `autoplay_suspended` is set and cleared only by the events of invariant 7; `ResumeAudio` on a moment does not clear it.
5. [ ] Session Pause and End stop only guide audio; Start while a moment plays injects `playingNow` and waits for the player to be free.

## Out of scope

`services/audio` (G05.03), Moment UI and Nearby (G07.02–G07.03), analytics events.

## Proof

Let `MomentFinished` add to `heard` → the teaser test fails. Showboat: required.

## Result and handoff

Write `docs/agent-tasks/results/G05.01.c.md` with exact status, files, commands and expected/actual outcomes.
