# G05.01.d — Model parity and invariant properties

**Status:** not-started. **Model class:** strong. **Dependencies:** [G05.01.c](G05.01.c.md) complete. Parent task: G05.01.

## Scope

Prove that the production reducer gives the same answers as the documentation model, and turn the invariants of 09 §6.1 into property tests, so the model can later be frozen as history ([run-model README](../../run-model/README.md), «Як перанесці ў дадатак»).

Read: `docs/run-model/run-model.mjs`, `run-model.test.mjs`, `check-regressions.mjs`; [09 §6.1, §12](../../architecture/09_technical_architecture.md); [19 §7.2](../../architecture/19_class_and_module_map.md) (run-model is frozen; changes only through a separate task).

## Allowed outputs

- a parity harness and property tests in a location `.dependency-cruiser.cjs` allows without new baseline entries, wired into `npm test`
- a mutation runner that edits only temporary copies
- `docs/demos/<YYYY-MM-DD>-g0501d-model-parity.md`
- `docs/agent-tasks/results/G05.01.d.md`

## Acceptance

1. [ ] The scenarios of `docs/run-model/run-model.test.mjs` run against the production `step()` through one name adapter with the same results; the runner prints the count of ported scenarios, and it equals the model's count.
2. [ ] Property tests cover invariants 1–9 (seeded, deterministic), plus the equivalent of the model's depth-4 exhaustive enumeration.
3. [ ] Each of the model's 21 regression mutations has a production equivalent: the mutation applied to a temporary copy makes a named test fail; repository files stay unchanged.
4. [ ] run-model files are not edited; freezing the model is recorded in the results file as a follow-up for the document owner.
5. [ ] `npm run arch:check` is green.

## Out of scope

Simulator and field fixtures (G05.06), any change to run-model or its README, shared docs.

## Proof

The mutation run itself: every mutation must turn at least one named test red. Showboat: required.

## Result and handoff

Write `docs/agent-tasks/results/G05.01.d.md`; the parent G05.01 closes only after .a–.d are complete.
