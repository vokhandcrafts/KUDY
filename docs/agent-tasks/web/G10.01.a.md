# G10.01.a — Web scaffold and public-layer reader

**Status:** not-started. **Model class:** medium. **Dependencies:** G02.03 (issue #55) must be closed; nothing here may start from its WIP branch. Parent task: G10.01. Plan: [2026-09-16-web-audio-version](../../plans/2026-09-16-web-audio-version.md).

## Scope

Deliver only this task's result. Do not start G10.01.b or any publication work. Work in the current checkout; preserve unrelated edits and prior result files. Planning a file does not mean it already exists.

Read AGENTS.md, the [web plan](../../plans/2026-09-16-web-audio-version.md), [09 §2/§4/§8](../../architecture/09_technical_architecture.md), the [build-bundle README](../../../tools/build-bundle/README.md), [21 §3.2](../../architecture/21_discovery_feedback_architecture.md), and both repository review/proof policies.

The stack named in the plan (Next.js + TypeScript on Vercel) is a **proposal**. Record the founder's confirmation (or an explicit founder decision for an alternative) in the result file before scaffolding; do not treat the proposal as accepted.

## Allowed outputs

- `web/**` (new directory; app skeleton and `web/lib/content/**` readers)
- `web/.gitignore` or root `.gitignore` — web build/output directories in the same change that generates them
- `package.json` — only the `test` script glob wiring for the new suite
- `docs/agent-tasks/results/G10.01.a.md`

No deployment, no domain, no store links, no UI pages. G10.02.b owns publication.

## Interfaces

- Consumes: the build-bundle public output layout exactly as listed in its README (`public/bundle/<route_id>/<version>/…`, `public/places/<place_id>/public.json`, `public/discovery/<city_id>/<revision>/index.json`); `previews.json` fields «толькі `stop_id`, `place_id`, `name`, `announce`»; `DiscoveryIndexV1` per 21 §3.2; catalog fields per 09 §4. Fixture input is built with the merged build-bundle from `fixtures/content/demo-route/`.
- Produces: pure TypeScript readers (no React, no network at module scope) — catalog, discovery index, route (`route_id × version × locale`), stops, previews; a single app-links config module with placeholder values and typed shape (consumed by G10.01.b/G10.02.a); the leak guard described below.

## Steps

- [ ] 1. Record the stack decision and scaffold the web app so `next build` (or the chosen framework's equivalent) passes locally.
- [ ] 2. Implement the typed readers against the demo-route fixture output; validate every consumed JSON against its contract source; reject unknown `schema_version` safely (G02.05 rule) instead of rendering it.
- [ ] 3. Implement the leak guard over the web content input and rendered output: any `extended` path segment, private-text 8-gram, or `*.map` file fails the build with codes matching the build-bundle classes (`private-text-leak`, `private-path-in-public`, `source-map-in-public`).
- [ ] 4. Wire the new test suite into the default `npm test` command in the same change; the printed count must include it (runner wiring is a contract, implementation-rules §7).
- [ ] 5. Verify the guard by temporarily removing it and watching the test fail, before pushing.

## Acceptance

1. [ ] `npm test` runs the new web suite and prints it in the total; reverting the wiring makes the suite disappear from the count.
2. [ ] Readers return the demo-route fixture data with correct locale/tier projection; a modified `schema_version` input yields a defined safe rejection, not a partial render.
3. [ ] Feeding the guard an input containing an `extended` path or a source map fails the build; removing the guard fails the test suite.
4. [ ] No file outside the allowed outputs changed; no content copy is committed (generated content trees are gitignored in the same change).
5. [ ] The result file records the stack decision, exact commands, and expected/actual outcomes.

## Shared invariants

Only the public layer reaches the web. No private bytes, no feedback registry, no store URLs (placeholders only), no analytics, no accounts. The web does not mutate bundles; versioning and pointer updates belong to G02.04.

## Result and handoff

Write `docs/agent-tasks/results/G10.01.a.md` with exact status, files, commands, expected/actual outcomes and the next eligible task. Apply `agent-rules/showboat.md` (a build/leak-guard demo qualifies when the diff reaches its thresholds) and `agent-rules/code-review.md` self-review before reporting done.
