# G10.02.a — App transition: links, QR, no dead ends

**Status:** not-started. **Model class:** medium. **Dependencies:** G10.01.c complete. Parent task: G10.02. Plan: [2026-09-16-web-audio-version](../../plans/2026-09-16-web-audio-version.md).

## Scope

Deliver only this task's result. Do not publish, do not register domains, do not fabricate store URLs. Work in the current checkout; preserve unrelated edits.

Read the [web plan](../../plans/2026-09-16-web-audio-version.md), [06 §3 (link/QR channel)](../../06_brand_and_promotion.md), [01 «Дзе відаць цана»](../../01_product_structure.md), [16 row G10.02](../../16_delivery_backlog.md), and the G10.01 result files.

## Allowed outputs

- `web/**` — `/app` fallback page, QR asset generation for route URLs, OG/Twitter card metadata
- `web/config/app-links.*` — the single app-transition config (extend the G10.01.a placeholder)
- `docs/agent-tasks/results/G10.02.a.md`

No deployment (G10.02.b), no real store URLs (they do not exist), no analytics.

## Interfaces

- Consumes: G10.01.a app-links config; G10.01.b CTA block; catalog `product_id` fields (09 §4) for future per-route deep links.
- Produces: the app-transition contract every page uses; QR assets; card metadata built from public fields only.

## Steps

- [ ] 1. Finalize the app-links config as the only source of app destinations: store URLs (placeholder values while the app is unpublished, typed so a real URL cannot be confused with a placeholder), future deep-link base, per-route product references.
- [ ] 2. `/app` page (be/en): explains what the app adds (GPS-аўтазапуск, офлайн, поўныя маршруты) and renders store buttons from the config; missing URLs render an explicit «хутка» state — 16 G10.02: «неўсталяваны дадатак не дае тупік».
- [ ] 3. QR generation: static QR assets for route URLs, suitable for print; the encoded URLs go through the same config/URL builder, never hand-typed.
- [ ] 4. OG/Twitter card metadata per route and stop from public fields only (cover, name, announce, duration); 16 G10.02: «метададзеныя не раскрываюць private».
- [ ] 5. Committed scan test: no external app/store URL may appear outside the app-links config (reverted-line check, implementation-rules §1); run the leak scan over generated metadata.

## Acceptance

1. [ ] Every paid-content CTA on every page resolves through the one config; the scan test fails if a URL is hardcoded elsewhere.
2. [ ] With placeholder store URLs, all CTAs still land on the `/app` page with no broken links or dead ends.
3. [ ] QR assets generate for every published route URL and decode back to the same URL.
4. [ ] Card metadata for a route with locked stops contains only public fields (verified against the fixture previews).
5. [ ] `npm test` includes the new cases; the result file records evidence per criterion.

## Shared invariants

The web never promises what only the app does (GPS, offline, purchases). Store URLs are not invented. Metadata is public-only. The URL scheme stays stable for M7 deep links.

## Result and handoff

Write `docs/agent-tasks/results/G10.02.a.md` with exact status, files, commands and expected/actual outcomes; apply Showboat and the code-review self-review before reporting done.
