# G10.01.b — Catalog page, guide page and city map (BE/EN)

**Status:** not-started. **Model class:** medium. **Dependencies:** G10.01.a complete; G01.04 (navigation contract) closed. Parent task: G10.01. Plan: [2026-09-16-web-audio-version](../../plans/2026-09-16-web-audio-version.md).

## Scope

Deliver only this task's result. Do not start stop pages, the audio player or publication. Work in the current checkout; preserve unrelated edits.

Read the [web plan](../../plans/2026-09-16-web-audio-version.md), [01 «Дзе відаць цана» and IA](../../01_product_structure.md), [09 §6.3.1/§6.5/§8](../../architecture/09_technical_architecture.md), [16 rows G10.01/G01.04](../../16_delivery_backlog.md), and the G10.01.a result file.

## Allowed outputs

- `web/**` — home/catalog page, guide page, static city map page, shared layout, locale routing, UI string files (`be`, `en`)
- `docs/agent-tasks/results/G10.01.b.md`

No audio playback logic, no QR, no store URLs, no deployment.

## Interfaces

- Consumes: G10.01.a readers and app-links config; `previews.json` fields («толькі `stop_id`, `place_id`, `name`, `announce`»); `DiscoveryIndexV1` availability/access fields (21 §3.2); public place projections (`public/places/<place_id>/public.json`).
- Produces: locale-prefixed routes (`/`, `/be/…`, `/en/…` per the plan's language section) and a reusable locked-stop row + calm-offer CTA block consumed by G10.01.c.

## Steps

- [ ] 1. Build the city catalog page from the discovery index and catalog: only real published entries; with one guide, one card plus the map entry — «EXPLORE не імітуе каталог» (01).
- [ ] 2. Build the guide page: cover, languages/duration/distance/stop count from the bundle, ordered stop list; locked stops show padlock + `name` + `announce` only, never a truncated paid text.
- [ ] 3. Build the static city map page: markers of the published free routes and their stops, manual overview only — no position, no «nearby» personalization. MapLibre GL JS or equivalent; ODbL attribution visible and clickable (09 §6.3.1); the tile provider is recorded as a decision (self-hosted or licensed provider — public OSM tile servers ban bulk scraping; no offline requirement exists on the web).
- [ ] 4. Apply the price rule (01): one calm offer block at the bottom of the guide description pointing to the app-links config; no popups, no banners, no interruptions.
- [ ] 5. UI strings in `be` and `en` files from the first page; no hardcoded copy; per-locale availability is shown by fact (a text-only locale never claims audio it does not have).
- [ ] 6. Assert at build time that the rendered HTML for every page contains no `extended` references; extend the G10.01.a leak scan to cover rendered output.

## Acceptance

1. [ ] Home, guide and map pages render the demo-route fixture in `be` and `en`; switching locales never substitutes a close language automatically.
2. [ ] A locked stop renders exactly the four public preview fields plus the padlock; grep of the rendered HTML finds no paid text.
3. [ ] With one fixture guide the home shows that card and the map entry — no placeholder catalog cards.
4. [ ] The map shows ODbL attribution on screen and loads tiles from the recorded provider only; no marker starts playback or links to paid content.
5. [ ] Every app CTA resolves through the single app-links config; removing the config breaks the build or the test, not silently the link.
6. [ ] `npm test` still prints all suites including the new page/leak assertions; the result file records evidence per criterion.

## Shared invariants

No GPS, no autoplay, no session, no accounts, no analytics, no checkout. Locked content exists only as the four-field public preview. The guide page is a preview of the walk, not a second product: download/start/purchase flows do not exist on the web.

## Result and handoff

Write `docs/agent-tasks/results/G10.01.b.md` with exact status, files, commands and expected/actual outcomes; apply Showboat and the code-review self-review before reporting done.
