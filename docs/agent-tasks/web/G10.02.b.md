# G10.02.b — SEO, publication and rollback

**Status:** not-started. **Model class:** medium. **Dependencies:** G10.02.a complete; G03.05 (first published guide and the verified free web layer) closed — real content exists. Parent task: G10.02. Plan: [2026-09-16-web-audio-version](../../plans/2026-09-16-web-audio-version.md).

## Scope

Deliver only this task's result. This is the public launch task: the web channel goes live. Work in the current checkout; coordinate any Vercel/domain access with the operator (external prerequisites mean `blocked-external`, never a mock).

Read the [web plan](../../plans/2026-09-16-web-audio-version.md), [09 §13 M3](../../architecture/09_technical_architecture.md), [16 rows G10.02/G03.05/G11.04](../../16_delivery_backlog.md), and the G10.01/G10.02.a result files.

## Allowed outputs

- `web/**` — sitemap/robots, SEO metadata completion, publication configuration
- CI/deploy configuration for the web project (Vercel project wiring; no edits to `.github/` workflows beyond what the operator authorizes — never edit protected CI yourself)
- `docs/agent-tasks/results/G10.02.b.md`

No content changes, no app-side changes, no store metadata (G11.04 owns store claims).

## Interfaces

- Consumes: the published catalog and public bundles from G02.04's publication flow; the G10.02.a transition contract.
- Produces: the live web channel; the publication/rollback drill evidence G11.03/G11.04 will reference.

## Steps

- [ ] 1. SEO: sitemap listing only published routes/locales, robots, canonical URLs, indexable server-rendered transcripts (M3: «тэкст індэксуюцца»).
- [ ] 2. Publication flow: a content publish (new catalog pointer by G02.04) triggers the web rebuild; the web never bundles content itself.
- [ ] 3. Rollback drill: prove that a failed publication leaves the previous release serving (16 G10.02: «пры збоі публікацыі працуе папярэдні рэліз») — run it once for real and record the evidence.
- [ ] 4. Post-deploy leak scan over the deployed HTML/assets/metadata: zero `extended` references, zero private text, zero source maps.
- [ ] 5. Platform claims (domain, hosting, indexing status) are recorded with the actual command/output or marked not verified — never stated as prose facts (implementation-rules §9).

## Acceptance

1. [ ] The production URL serves the published free guide in `be`/`en`; the link opens and audio plays (M3 acceptance).
2. [ ] The sitemap lists exactly the published routes/locales from the catalog — no draft or locked-only routes.
3. [ ] The rollback drill evidence is recorded: a broken publish attempt left the previous release serving.
4. [ ] The deployed-output leak scan returns zero hits and is wired as a repeatable command.
5. [ ] The result file records the deployment state with verifiable outputs and the honest not-verified list.

## Shared invariants

Only the public layer is ever deployed. No analytics without the 09 §10 consent rules. Store claims stay with G11.04. The channel remains deliberately flat: no GPS, no offline, no accounts.

## Result and handoff

Write `docs/agent-tasks/results/G10.02.b.md` with exact status, files, commands and expected/actual outcomes; apply Showboat and the code-review self-review before reporting done. Parent G10.02 completes only when both a and b are complete.
