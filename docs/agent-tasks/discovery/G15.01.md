# G15.01 — Pure authored discovery selection

**Status:** not-started. **Model class:** strong. **Dependencies:** G01.06, G02.01, G00.04.

## Scope

Deliver only this task's result. Do not start successors. Work in the current checkout, preserve unrelated edits and prior result files. Planning a file does not mean it already exists.

Read AGENTS.md, [business spec](../../20_discovery_and_feedback.md), [technical spec](../../architecture/21_discovery_feedback_architecture.md), [implementation plan](../../plans/2026-09-13-discovery-feedback.md), [backlog](../../16_delivery_backlog.md), and both repository review/proof policies. Human-facing documents are Belarusian; agent results/briefs are English.

Check `docs/agent-tasks/results/<ID>.md` and each referenced artifact for every predecessor. A missing, decision-required or blocked-external result is not complete. Parent IDs require their existing atomic children; do not treat a parent brief as evidence.

## Allowed outputs

- `core/discovery/selectDiscovery.ts`
- `core/discovery/selectDiscovery.test.ts`
- `docs/agent-tasks/results/G15.01.md`

Runtime tasks may also create their own policy-required Showboat demo and narrowly scoped tests. If a listed path is a directory placeholder, resolve actual file names from the completed scaffold/design and record that concrete list before editing. No rewrite of adjacent features.

## Interfaces

- Consumes: DiscoveryIndexV1 and DiscoveryCriteria from contracts/discovery.ts; accepted contract fixture set.
- Produces: selectDiscovery(index, criteria): DiscoveryResult with exact/alternatives and typed reasons/differences, as defined in 21 §4.

## Steps

- [ ] 1. Inspect production test scripts established by G00.04; if absent, do not install or invent a second stack.
- [ ] 2. Write failing behavioral tests using fixtures A–H from the implementation plan; ensure inputs remain unchanged.
- [ ] 3. Implement the pure selector using the specified hard exclusions, theme OR, condition AND, upper duration bound and stable editorial ordering.
- [ ] 4. Prove that removing paid flags or changing a hypothetical score does not change order; no ratings input is accepted.
- [ ] 5. Run focused tests and applicable type/lint checks; record runtime proof under the repository policy.

## Acceptance

1. 60 minutes rejects [45,75] from exact; 120 accepts it.
2. Unknown time becomes an explicitly labeled alternative only.
3. Wrong city or explicit content locale is excluded even from alternatives.
4. One exact result stays alone; zero exact does not silently widen conditions.
5. Same input and ordering ties always produce the same offer IDs; duplicate refs are not displayed twice.

## Shared invariants

No runtime LLM, weather engine, public ratings, user-profile inference or automatic rating-based ranking. No new GPS/player owner. No disclosure of paid content from public previews. A collection cannot start a Run or create an entitlement. Feedback is voluntary, authenticated to the submitting installation and separate from analytics consent. Never log coordinates, secrets or free-text feedback. Unknown locale/time/season is not silently accepted as a match.

## Result and handoff

Write `docs/agent-tasks/results/G15.01.md` with exact status, files, predecessor evidence, commands, expected/actual outcomes, limitations and the next eligible task. For runtime work, record a failing behavioral case before the fix and the same passing case afterward. Run actual applicable type/lint/tests and Showboat; do not install a toolchain just for Markdown. Do not label synthetic/desktop checks as mobile, database or store evidence. Required unavailable prerequisites mean blocked-external or decision-required, never manufactured complete.

