# G10.01.b — catalog and guide pages, rendered-output leak scan

*2026-09-18T22:09:42Z by Showboat 0.6.1*
<!-- showboat-id: cdb272b5-3c6a-4b51-91b9-a35f91d38425 -->

The web pages read the same public bundles through the G10.01.a readers: the home builds its one real card from the catalog and the discovery index, and per-locale availability is shown by fact (uk is text-only — no audio is claimed). *(2026-09-20: the `map entry` print was removed by TR-8 of docs/architecture/23_technical_remarks.md — the catalog no longer carries mapHref, the dead /map link stays hidden until the tile-provider decision.)*

```sh
node --experimental-strip-types -e "Promise.all([import('./web/lib/content/site.ts'),import('./web/lib/content/test-fixture.ts')]).then(async([s,f])=>{const {publicRoot}=await f.buildDemoFixture();const home=s.readSiteCatalogPage(publicRoot,'be');console.log('cards:',home.cards.length,'title:',home.cards[0].title,'href:',home.cards[0].href);console.log('languages:',home.cards[0].languages.map(l=>l.locale+(l.audio?'(audio)':'(text)')).join(','));const g=s.readSiteGuidePage(publicRoot,'be','demo-route-a1');const locked=g.stops[1];console.log('stops in order:',g.stops.map(x=>x.stop_id).join(','));console.log('locked row fields:',Object.keys(locked).sort().join(','));console.log('locked preview:',locked.name,'|',locked.announce)})"
```

```output
cards: 1 title: Дэма-гід: сукнаны двор href: /guides/demo-route-a1
languages: be(audio),en(audio),uk(text)
stops in order: stop-1,stop-2
locked row fields: announce,locked,name,place_id,stop_id
locked preview: Млынавая калона (дэма) | За паваротам — апошняя калона старога млына і гісторыя пра яе вяртуна.
```

The build derives the interim catalog (publication is G02.04), exports the static pages and runs the leak scan over the rendered output — G10.01.b step 6 wired into the build script itself.

```sh
cd web && npm run build 2>&1 | grep -E "content root ready|Exporting|rendered-output scan|✓ Generating"
```

```output
content root ready: 1 route(s), discovery discovery/demo-city/r-demo-1/index.json
 ✓ Generating static pages (7/7)
   Exporting (0/2) ...
 ✓ Exporting (2/2)
rendered-output scan: clean
```

The rendered HTML carries the locked stop as the padlock plus name and announce only — and no paid text anywhere. The en guide is exported under its locale prefix.

```sh
node --experimental-strip-types -e "const fs=require('fs');const g=fs.readFileSync('web/out/guides/demo-route-a1.html','utf8');const ext=JSON.parse(fs.readFileSync('fixtures/content/demo-route/be/extended/stops.json','utf8'))[0];console.log('padlock on guide page:',g.includes('🔒'));console.log('locked preview shown:',g.includes('Млынавая калона (дэма)'));console.log('calm offer shown:',g.includes('Поўная версія — у дадатку'));console.log('en guide exported:',fs.existsSync('web/out/en/guides/demo-route-a1.html'));console.log('paid text hits:',g.includes(ext.text)||g.includes(ext.transcript)?1:0)"
```

```output
padlock on guide page: true
locked preview shown: true
calm offer shown: true
en guide exported: true
paid text hits: 0
```

The new suites are wired into the default test command (runner wiring is a contract): page data, the rendered-output scan and the wiring guards run through node's type stripping.

```sh
node --test --experimental-strip-types --test-reporter=tap --test-concurrency=1 web/lib/content/pages.test.ts web/lib/content/rendered-leak.test.ts web/lib/content/wiring.test.ts 2>&1 | grep -E '^# (tests|pass|fail)'
```

```output
# tests 18
# pass 18
# fail 0
```
