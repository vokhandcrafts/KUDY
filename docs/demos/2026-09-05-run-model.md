# Run documentation model: executable checks

*2026-09-05T21:23:59Z by Showboat 0.6.1; re-captured 2026-09-24 — the suite and regression counts are live output, not maintained claims (G18.05.c)*
<!-- showboat-id: 837fb02d-f49f-4a64-b10f-81fd1d4f5722 -->

Documentation model only: the run-model `node --test` suite, including 4680 bounded state transitions. No device, GPS, audio adapter, SQLite, payment or UI verification. Coverage and assumptions: docs/run-model/README.md.

```python
import subprocess; subprocess.run(["node", "--test", "--test-reporter=dot", "docs/run-model/run-model.test.mjs"], check=True)
```

```output
....................
....................
....................
.......
```

```python
import subprocess; subprocess.run(["node", "docs/run-model/check-regressions.mjs"], check=True)
```

```output
CAUGHT: consume queued stop before playback
CAUGHT: erase heard during replay
CAUGHT: autoplay manually completed stop
CAUGHT: credit extended together with base
CAUGHT: swap primary to extended after unlock
CAUGHT: accept completion from another session
CAUGHT: accept completion from earlier playback
CAUGHT: validate story id by truthiness instead of presence
CAUGHT: accept a foreign moment completion
CAUGHT: credit a moment teaser to guide history
CAUGHT: resume a stale or closed launch
CAUGHT: stop a moment on session pause
CAUGHT: treat a manual pause as a full stop
CAUGHT: accept stale queued location
CAUGHT: play locked stop manually
CAUGHT: autoplay locked stop
CAUGHT: activate a different content version
CAUGHT: accept a grant for another route
CAUGHT: accept a grant for another locale
CAUGHT: accept a grant from a foreign issuer
CAUGHT: start without verified layers
21/21 reviewed regressions rejected. Repository model unchanged.
```
