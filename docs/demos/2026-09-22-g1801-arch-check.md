# G18.01 — arch:check gate: green at HEAD, blocks new violations, baseline passes known

*2026-09-22T21:05:12Z by Showboat 0.6.1 (re-captured after G18.03 added tools/arch-surface: tools-zone scan 104 → 115 modules)*
<!-- showboat-id: 1f683ed4-1a5d-4d8b-b3cf-a61e2d84d4e9 -->

The machine check of the 09 §6 / 19 §2 layer matrix (dependency-cruiser via
`npm run arch:check`): green on the repository with the six consciously
baselined `web` ↔ `tools` imports; a planted `core/` production file importing
`node:fs` fails with the rule and path named; removing the planted file
restores green (the demo leaves the tree unchanged).

```sh
npm run arch:check; echo "exit=$?"
```

```output

> kudy@0.1.0 arch:check
> node tools/arch/arch-check.mjs --config .dependency-cruiser.cjs --baseline tools/arch/baseline.json core services contracts tools web

arch:check: OK — 115 modules cruised, 6 violation(s) pass via the baseline dated 2026-09-22
exit=0
```

A new violation is never silently accepted: `core/tmp-demo-violation.mjs`
imports `node:fs` — the gate exits nonzero naming `core-zone-closed`.

```sh
printf 'import fs from "node:fs";\nexport const x = fs;\n' > core/tmp-demo-violation.mjs
npm run arch:check; echo "exit=$?"
```

```output

> kudy@0.1.0 arch:check
> node tools/arch/arch-check.mjs --config .dependency-cruiser.cjs --baseline tools/arch/baseline.json core services contracts tools web

arch:check: FAIL — 1 violation(s) not in the baseline dated 2026-09-22:
- core-zone-closed: core/tmp-demo-violation.mjs -> fs
Fix the import; baseline an existing violation only consciously, in review (tools/arch/README.md).
exit=1
```

Cleanup restores the green state (the planted file is removed).

```sh
node -e "require('fs').unlinkSync('core/tmp-demo-violation.mjs')"
npm run arch:check; echo "exit=$?"
```

```output

> kudy@0.1.0 arch:check
> node tools/arch/arch-check.mjs --config .dependency-cruiser.cjs --baseline tools/arch/baseline.json core services contracts tools web

arch:check: OK — 115 modules cruised, 6 violation(s) pass via the baseline dated 2026-09-22
exit=0
```
