# G06.08 — interactive prototype walkthrough on the normative model

*2026-09-22 by Showboat 0.6.1*
<!-- showboat-id: g0608-prototype-walkthrough -->

Issue #61 criterion 1 and criterion 5: the whole Start → manual Play → pause →
upgrade → return from Moment → other-guide hint (R07) → End chain runs on the
accepted model (docs/run-model/run-model.mjs), discovery D01–D07 through the
real selector over the accepted fixture copy, feedback states F01–F04 and the
L01 availability fact. The interactive browser surface
(spikes/G06.08-prototype/) renders exactly this state machine.

```sh
node --experimental-strip-types spikes/G06.08-prototype/prototype/walkthrough.mjs 2>/dev/null
```

```output
1 Start route-free-1 v1 be: Active, accessible=[stop-1,stop-2], stop-3 locked (paid-only, base only verified)
2 DwellCompleted stop-1: playing={guide,Стары кран,playId=1}, marker=playing, commands=PlayStory
3 UserPausedAudio→ResumeAudio: live pause kept token playId=1 (true), suspended=false, heard=0
4 DwellCompleted stop-2 while playing: queued={"stopId":"stop-2","radius":40,"at":1002500}, marker stop-2=pending (still pending, auto_fired untouched)
5 PlayMoment moment-m1: guide stopped by command, queue retired to auto_fired (true), suspended=true; MomentFinished: heard=[] (moment never credits guide)
6 «Працягнуць гід»: suspended=false, playing=null (automation back, nothing sounds by itself)
7 Manual Play stop-2 → finished: heard=["story-free-2-base"], marker=played
8 AccessReady extended (upgrade): stop-3 pending now unlocked, heard/auto_fired untouched=1/2, no Play issued=true
9 Extended stop-1 with a phone call: FocusLoss→FocusRegain (+50s) keeps the live pause, ResumeAudio resumes the same token, finished → heard=["story-free-2-base","story-free-1-ext"], marker stop-1=available
10 Pause walk: state=Paused, playing=null, commands=ClearGeofences|UnsubscribeLocation|ReleaseWakelock
11 Resume walk: state=Active
12 End: state=Ended, «Яшчэ можна адкрыць»=["story-free-1-base","story-free-3-ext"] (unit story, locked excluded)
13 R07 quiet hint target: route-free-2 free, one factual show per session (UI-tracked), tap opens preview only — model untouched
14 R07 eligibility predicate (ui-rules): playing=false, paused=false, suspended=false, idle=true (false everywhere but idle Active)
D03@60: exact=[offer-a1-place,offer-g1-place,offer-h1-place] (paid 45–75 excluded), b1=over_time
D03@120: exact=[offer-b1-guide,offer-a1-place,offer-f1-collection,offer-g1-place,offer-h1-place], first=editorial+theme_match+within_time
D04 zero exact (theme-sea @15): exact=[], alternatives=offer-b1-guide(over_time+theme_mismatch), offer-a1-place(over_time+theme_mismatch), offer-c1-place(duration_unknown), offer-f1-collection(over_time), offer-g1-place(over_time+theme_mismatch), offer-h1-place(over_time+theme_mismatch)
D05 autumn: exact=[offer-h1-place] (h1 has the season reason, a1/g1 stay labeled)
D06 paid marking: b1 access=paid; tap opens preview, never buys/starts/unlocks private
D07/D02 paths: collection-f1 members=[place+guide] open their own cards, no Start/audio; guide-route-a1 preview is one shared card from rubric, collection and hint
L01 availability: text=be,en,uk audio=be,en → uk text without uk audio gives no Start promising ukrainian audio
F02 targets: guide {kind:guide,route_id,version,locale} vs place {kind:place,place_id,content_version,locale} — separate keys, never aggregated
F02 form: score 1–5 no default, max 3 unique reasons; guide=[interesting_stories, clear_delivery, too_long, hard_to_navigate, audio_problem, description_mismatch], place=[worth_visiting, description_mismatch, hard_to_reach, access_problem]
F03 online: draft → pending → sending → sent; offline restart: draft kept, draft → pending → conflict → action_required resolves via CAS revision
F01 skip: no record, nothing blocked; edit replaces (same target key), delete leaves tombstone, report shows count+version+language
```
